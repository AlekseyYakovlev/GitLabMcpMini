---
status: complete
---
# Quick 260926-24l Summary

- 403 on create_branch / commit_files / create_or_update_file / create_merge_request /
  update / merge / note now triggers one best-effort GET of the project. Priority:
  marked for deletion, archived, role below Developer, else the old generic text.
  A failed lookup leaves the generic text; raw GitLab bodies are never shown.
- get_project prints `marked_for_deletion_on` (only when set) and `your_access`
  (max of project/group access, "unknown" without permissions).
- list_projects appends `[scheduled for deletion]` / `[archived]`. `simple=true` was
  removed: the simple projection omits both fields, so the markers need the full
  entity (larger response; still capped by Budget). No order_by/sort added.
- Tool count unchanged (20). Descriptions of get_project/list_projects and the
  golden file updated; README gained the two mentions.
- `go vet ./...` and `go test ./...` pass.
- Live gitlab.com, project 86873071: get_project showed `marked_for_deletion_on:
  2026-09-25` and `your_access: Maintainer (40)`; create_branch returned
  "403: проект запланирован к удалению (2026-09-25), запись невозможна";
  list_projects marked the project. The token in the environment was a
  Maintainer-level token, not the bot's project token, so the Reporter path was
  covered by unit tests only.

Code commit: 2b007e6
