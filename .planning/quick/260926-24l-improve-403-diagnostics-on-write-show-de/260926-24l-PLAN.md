# Quick 260926-24l: 403 diagnostics on write, deletion state and role in projects

Goal: a 403 on a write names its real cause; get_project/list_projects show deletion
state and the token's role. No new tools (stay at 20).

Tasks (executed inline, no subagents):
1. `internal/tools/access.go`: accessLevel/roleName/yourAccess/forbiddenReason and
   `Deps.explainForbidden` (best-effort GET /projects/:id, 5 s cap).
2. `errors.go` (`project`/`reason` on subjectError, `withProject`), the six write
   call sites, `safe.go` calls explainForbidden after deadlineAsServerError.
3. `projects.go`: marked_for_deletion_on and your_access in get_project; markers in
   projectLine; drop `simple=true` (it hides archived and marked_for_deletion_on).
4. Tests `access_test.go` (each reason, failing lookup, other writes, read not
   explained, get_project, list_projects); golden and README updated.
5. Live check on gitlab.com project 86873071 via a scratch mcp==1.30.0 client.
