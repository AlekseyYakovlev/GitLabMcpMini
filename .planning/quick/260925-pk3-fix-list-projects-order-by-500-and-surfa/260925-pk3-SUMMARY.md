---
status: complete
---

# Quick 260925-pk3 — Summary

## Сделано
- `2af4fdd` — `list_projects` больше не шлёт `order_by=last_activity_at`; тест проверяет, что в запросе нет `order_by`/`sort`, а `membership`, `simple`, `page`, `per_page` прежние.
- `9c1a041` — если GET упёрся в дедлайн вызова, а последним ответом был 5xx, пользователь видит `500: ошибка сервера GitLab, повторите позже.` вместо «Превышено время ожидания». Политика retry не менялась; записи (`withWrite`) сохраняют прежние формулировки.
- Описание инструмента и golden не менялись (сортировку «по активности» они не обещают; в `get_project` «дата последней активности» — поле ответа).

## Проверено
- `go vet ./...`, `go test ./...` (включая e2e и golden tools/list) — зелёные. Сквозной тест дедлайна на фейковом GitLab падает без правки в `safe.go` и проходит с ней.
- Живьём на gitlab.com (только чтение, токен из окружения): `uv run scripts/smoke.py --project AlekseyYakovlev/sanbox` — `SMOKE OK`; `list_projects` без ошибки вернул `AlekseyYakovlev/sanbox`, 0.62 / 0.37 / 0.28 с.

## Только на подделке (не воспроизведено живьём)
- Сообщение «500: ошибка сервера GitLab…» при дедлайне: проверено фейковым GitLab, на реальном gitlab.com 500 больше не воспроизводится, потому что `order_by` не отправляется.
- Ветка записей (`deadlineAsServerError` не подменяет write-ошибки) — юнит-тест; запись на реальный GitLab не запускалась.

## Напоминание
Агент AiAdventAgentV2 использует `C:\Projects\GitLabMcpMini\gitlab-mcp.exe`: чтобы подхватить новую сборку, отключите и заново подключите MCP-сервер GitLabMcpMini в настройках агента (или перезапустите приложение).
