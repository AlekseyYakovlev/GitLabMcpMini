# Quick 260925-pk3: list_projects — убрать order_by, показывать реальный 5xx

1. `internal/tools/projects.go`: не передавать `order_by` (gitlab.com отвечает 500 на `last_activity_at`); тест проверяет отсутствие `order_by`/`sort`.
2. Описание list_projects и `tools_list.golden.json` не обещают сортировку по активности — менять не нужно.
3. Дедлайн вызова после 5xx на GET сообщает реальный статус: `glclient.ServerStatus` в контексте вызова (пишет `checkRetry`, только GET/HEAD), подстановка в `safe()`; записи не затрагиваются.
4. `go test ./...`, пересборка `gitlab-mcp.exe`, живая проверка только на чтение.
