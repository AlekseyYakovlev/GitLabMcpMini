---
phase: 04-live-verification-and-delivery
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - .planning/phases/04-live-verification-and-delivery/LIVE-RUN.md
  - internal/tools/errors.go
  - internal/tools/errors_test.go
  - scripts/live.py
autonomous: false
requirements: [QA-03, QA-04]

must_haves:
  truths:
    - "gitlab-mcp.exe in the repo root is rebuilt with the canonical command after Plan 01's fix, and `go test ./...` and `go vet ./...` are green before any live write (D-12, D-15)"
    - "A live run of scripts/live.py against AlekseyYakovlev/sanbox with the author's GITLAB_TOKEN (api) completes read -> branch -> commit -> MR -> note -> merge -> cleanup with every step PASS (QA-03, ROADMAP criterion 1, D-05, D-09)"
    - "The same run shows understandable real errors: read_api token write -> 403 with scope `api` hint, nonexistent project -> 404, invalid token -> 401 without echo, duplicate branch/MR, 404 file/ref, Draft and merged-MR merge refusals, empty content (ROADMAP criterion 2, D-06, D-08)"
    - "Any divergence found by the live run is fixed in Phase 4 with a hermetic regression test on the fake and the run is repeated until green (D-04)"
    - "LIVE-RUN.md committed as evidence records the date, exe size + SHA-256 + go version -m excerpt (CGO_ENABLED=0), all steps with PASS status, the cleanup section, and contains no token (D-03, D-13)"
    - "Cleanup left no live/<ts>- branch and no open MR from the run in the sandbox (D-07)"
  artifacts:
    - path: ".planning/phases/04-live-verification-and-delivery/LIVE-RUN.md"
      provides: "redacted transcript of a green live run"
      contains: "cleanup (вне сервера)"
  key_links:
    - from: "scripts/live.py"
      to: "gitlab.com project AlekseyYakovlev/sanbox"
      via: "uv run scripts/live.py --project AlekseyYakovlev/sanbox"
      pattern: "AlekseyYakovlev/sanbox"
    - from: "LIVE-RUN.md"
      to: "gitlab-mcp.exe"
      via: "SHA-256 recorded in the header equals the rebuilt exe"
      pattern: "SHA-256"
---

## Phase Goal

**As a** автор агента AiAdventAgentV2, **I want to** прогнать весь сценарий `gitlab-mcp.exe` на реальном gitlab.com и получить готовый `.exe` с README, **so that** я уверен, что сервер работает вживую, и могу подключить его к агенту без догадок.

<objective>
Close the phase: rebuild the exe, get the author's tokens into the run (human action — the executor's shell has neither GITLAB_TOKEN nor GITLAB_TOKEN_READONLY), run `scripts/live.py` against the sandbox `AlekseyYakovlev/sanbox`, fix whatever real gitlab.com exposes (D-04) with hermetic tests, repeat until green, and commit `LIVE-RUN.md` as the evidence for QA-03 (and the build proof part of QA-04).

Purpose: Core Value is "works on real gitlab.com"; this is the only place it is proven.
Output: committed LIVE-RUN.md; possible small fixes in internal/tools (typically errors.go wording) with regression tests.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-live-verification-and-delivery/04-CONTEXT.md
@.planning/phases/04-live-verification-and-delivery/04-RESEARCH.md
@.planning/phases/04-live-verification-and-delivery/04-01-SUMMARY.md
@.planning/phases/04-live-verification-and-delivery/04-02-SUMMARY.md
@.planning/phases/04-live-verification-and-delivery/04-03-SUMMARY.md

<interfaces>
- Run command: `uv run scripts/live.py --project AlekseyYakovlev/sanbox` (spelling "sanbox" is the real URL, D-05). Exit 0 = green (read-only block may be SKIPPED only without GITLAB_TOKEN_READONLY -> criterion 2 PARTIAL), 1 = FAIL, 2 = usage/env.
- Transcript: `.planning/phases/04-live-verification-and-delivery/LIVE-RUN.md` (overwritten on every run).
- Canonical build (bash, used by the executor's Bash tool): `CGO_ENABLED=0 go build -trimpath -ldflags "-s -w" -o gitlab-mcp.exe ./cmd/gitlab-mcp`.
- Likely D-04 fix point (RESEARCH Pitfall 6): a `writeRules` entry in internal/tools/errors.go keyed on the lowercase substring `insufficient_scope`, with a hermetic case in errors_test.go. Do not add it unless the live text is actually unclear.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the exe canonically and run the full hermetic gate</name>
  <files>gitlab-mcp.exe (build output, gitignored — not committed)</files>
  <read_first>
    - .gitignore (confirm gitlab-mcp.exe / *.exe ignored)
    - .planning/phases/04-live-verification-and-delivery/04-01-SUMMARY.md (999.1 landed)
    - .planning/phases/04-live-verification-and-delivery/04-02-SUMMARY.md (live.py CLI as built)
  </read_first>
  <action>
Per D-12, from the repo root run `CGO_ENABLED=0 go build -trimpath -ldflags "-s -w" -o gitlab-mcp.exe ./cmd/gitlab-mcp`, then `go vet ./...`, `gofmt -l cmd internal e2e` (must print nothing) and `go test ./... -count=1` (no -race, D-15). Record the exe size and `sha256sum gitlab-mcp.exe` output in the plan SUMMARY (these must match the LIVE-RUN.md header later). Run `uv run scripts/live.py --help` to confirm the script starts. Check `git status --porcelain` shows no gitlab-mcp.exe entry. Do not run live.py with --project here (no token in this shell).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build -trimpath -ldflags "-s -w" -o gitlab-mcp.exe ./cmd/gitlab-mcp && go vet ./... && go test ./... -count=1 && uv run scripts/live.py --help</automated>
  </verify>
  <acceptance_criteria>
    - Build, vet and `go test ./... -count=1` exit 0; `gofmt -l cmd internal e2e` prints nothing
    - gitlab-mcp.exe mtime is newer than every *.go file (`find . -name '*.go' -newer gitlab-mcp.exe | wc -l` returns 0)
    - `git status --porcelain | grep -c gitlab-mcp.exe` returns 0
  </acceptance_criteria>
  <done>Fresh canonical exe exists; hermetic suite green; ready for the live run.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Author provides tokens and runs (or enables) the live run</name>
  <read_first>
    - README.md (section "Проверка", live run)
  </read_first>
  <action>
The executor cannot proceed alone: real PATs exist only with the author (D-06). Present the two options below and wait. Never ask the author to paste a token into the chat or into any file; never write a token to disk.
  </action>
  <what-built>Rebuilt gitlab-mcp.exe (Task 1) and scripts/live.py (Plan 02) that writes only to live/<ts>-* branches of the given project and cleans up after itself.</what-built>
  <how-to-verify>
    Option A (preferred — tokens never enter the agent's environment). In your own PowerShell window:
    1. `cd C:\Projects\GitLabMcpMini`
    2. `$env:GITLAB_TOKEN = "<PAT со scope api>"`
    3. `$env:GITLAB_TOKEN_READONLY = "<PAT только со scope read_api>"` (optional; without it criterion 2 becomes PARTIAL)
    4. Make sure the sandbox https://gitlab.com/AlekseyYakovlev/sanbox has README.md in main (D-05).
    5. `uv run scripts/live.py --project AlekseyYakovlev/sanbox`
    6. Reply with the exit code and the last line (LIVE OK / LIVE FAIL). LIVE-RUN.md is written into the repo.
    Option B: set both variables at user level (`[Environment]::SetEnvironmentVariable("GITLAB_TOKEN", "...", "User")`, same for GITLAB_TOKEN_READONLY), restart Claude Code so the executor's shell inherits them, and reply "tokens exported" — the executor then runs step 5 itself.
    Reminder (CONTEXT specifics): the tokens were pasted into a chat during discussion; revoke and reissue them after Phase 4.
  </how-to-verify>
  <resume-signal>Reply "ran: exit N" (Option A) or "tokens exported" (Option B)</resume-signal>
  <acceptance_criteria>
    - Option A: .planning/phases/04-live-verification-and-delivery/LIVE-RUN.md exists and its header date is today
    - Option B: `bash -c '[ -n "$GITLAB_TOKEN" ] && echo set'` prints "set" in the executor shell (value never printed)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f .planning/phases/04-live-verification-and-delivery/LIVE-RUN.md || [ -n "$GITLAB_TOKEN" ]'</automated>
  </verify>
  <done>Either a fresh LIVE-RUN.md from the author's run exists, or the executor shell has GITLAB_TOKEN (and optionally GITLAB_TOKEN_READONLY) without any token written to disk.</done>
</task>

<task type="auto">
  <name>Task 3: Evaluate LIVE-RUN.md, fix live findings with hermetic tests (D-04), repeat to green, commit evidence</name>
  <files>.planning/phases/04-live-verification-and-delivery/LIVE-RUN.md, internal/tools/errors.go, internal/tools/errors_test.go, scripts/live.py</files>
  <read_first>
    - .planning/phases/04-live-verification-and-delivery/LIVE-RUN.md (the whole transcript)
    - internal/tools/errors.go, internal/tools/mr_status.go (expected wording)
    - internal/testutil/fakegitlab.go, internal/tools/helpers_test.go (hermetic fixture API)
    - .planning/phases/04-live-verification-and-delivery/04-RESEARCH.md (Pitfalls 1, 4-7)
  </read_first>
  <action>
If Option B was chosen, run `uv run scripts/live.py --project AlekseyYakovlev/sanbox` yourself (timeout 600000 ms) — never echo token values.

Read LIVE-RUN.md. Classify every FAIL:
(a) server bug or unclear/wrong wording on real gitlab.com (e.g. the 403 read_api body `insufficient_scope` not explained) -> fix in internal/tools with a hermetic regression test on testutil.NewFakeGitLab reproducing the real response (status + body copied from the transcript, D-04), rebuild with the canonical command, `go test ./... -count=1`;
(b) live.py false failure (wrong regex, over-strict assert, timing) -> fix scripts/live.py, keeping every D-02/D-03/D-05/D-07 safety rule, and re-run `go test ./e2e -run TestLiveScriptUsage`;
(c) sandbox configuration (e.g. ci_must_pass, not_approved, README.md missing) -> not a code change: ask the author to adjust the sandbox (dynamic checkpoint).
After fixes, request another run (Option A: dynamic human-action checkpoint "re-run live.py"; Option B: run it). Repeat until exit 0. Budget: if more than 3 distinct server fixes are needed, or a fix touches more than 3 production files, stop and report that gap-closure planning is required (`/bm:plan-phase 4 --gaps`) instead of continuing inline. Every fix is committed separately (`fix(04-04): ...`) with its test; list any extra files touched in the SUMMARY.

When green: verify LIVE-RUN.md — no line has status FAIL; the Criterion 1 row is PASS; the Criterion 2 row is PASS (or PARTIAL only if the author explicitly declined to provide GITLAB_TOKEN_READONLY — record that in the SUMMARY); the header SHA-256 equals `sha256sum gitlab-mcp.exe`; the go version -m excerpt contains CGO_ENABLED=0; the cleanup section shows no "cleanup INCOMPLETE". Scan for secrets: `grep -cE 'glpat-[A-Za-z0-9_-]{20,}' LIVE-RUN.md` must be 0. Then commit ONLY that file by explicit path (`git add .planning/phases/04-live-verification-and-delivery/LIVE-RUN.md`; never `git add -A` or `.` — the untracked `.planning/.pending-auth-captures.jsonl` must not be committed) with message `docs(04): live run evidence on gitlab.com (QA-03)`.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/04-live-verification-and-delivery/LIVE-RUN.md; test -f $f && ! grep -qE "— FAIL|cleanup INCOMPLETE" $f && [ "$(grep -cE "glpat-[A-Za-z0-9_-]{20,}" $f)" = 0 ] && grep -q "CGO_ENABLED=0" $f && grep -q "cleanup (вне сервера)" $f && grep -q "$(sha256sum gitlab-mcp.exe | cut -d" " -f1)" $f && go test ./... -count=1 && echo GREEN'</automated>
  </verify>
  <acceptance_criteria>
    - Verify command prints GREEN
    - `git log -1 --name-only` for the evidence commit lists only LIVE-RUN.md
    - `git ls-files .planning/.pending-auth-captures.jsonl` prints nothing
    - Every fix commit (if any) includes a new/changed `_test.go` file reproducing the live response
  </acceptance_criteria>
  <done>A green live run on gitlab.com is committed as LIVE-RUN.md; every live finding is fixed with a hermetic test; the sandbox holds no leftover live branches or open MRs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> executor | Real PATs must reach the live run without passing through chat or files |
| live run -> gitlab.com sandbox | Real writes and merges |
| LIVE-RUN.md -> git history | Evidence becomes permanent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Information Disclosure | token handoff | mitigate | Option A keeps tokens in the author's own shell; never request tokens in chat or files; recommend revoke/reissue after the phase |
| T-04-18 | Information Disclosure | LIVE-RUN.md commit | mitigate | glpat- regex scan must be 0 before commit; stage by explicit path only; .pending-auth-captures.jsonl never staged |
| T-04-19 | Tampering | writes to real projects | mitigate | Only `--project AlekseyYakovlev/sanbox`; live.py refuses writes to the default branch; branches live/<ts>- only |
| T-04-20 | Tampering / DoS | cleanup | mitigate | Evidence requires no "cleanup INCOMPLETE"; cleanup deletes only tracked live/<ts>- branches (Plan 02) |
| T-04-21 | Tampering | inline D-04 fixes | mitigate | Each fix has a hermetic regression test and full `go test ./...`; >3 fixes escalates to gap-closure planning |
| T-04-22 | Information Disclosure | executor env (Option B) | accept | User-level env vars are the author's explicit choice; values are never echoed |
</threat_model>

<verification>
- LIVE-RUN.md committed, all PASS, no secrets, SHA matches the rebuilt exe
- `go test ./... -count=1` green after any fixes
</verification>

<success_criteria>
- ROADMAP Phase 4 criteria 1-3 evidenced by LIVE-RUN.md (+ TestBinaryHasNoExternalDependencies); criterion 4 by README from Plan 03
- QA-03 and QA-04 can be marked complete
</success_criteria>

<output>
Create `.planning/phases/04-live-verification-and-delivery/04-04-SUMMARY.md` when done
</output>
