---
phase: 04-live-verification-and-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tools/write.go
  - internal/tools/schema.go
  - internal/tools/write_test.go
  - internal/tools/upsert_test.go
  - internal/tools/testdata/tools_list.golden.json
  - .planning/ROADMAP.md
autonomous: true
requirements: [QA-03]

must_haves:
  truths:
    - "commit_files with a create or update action whose content is missing or empty returns isError containing 'непустой content' and sends ZERO requests to GitLab (D-10, backlog 999.1)"
    - "create_or_update_file with content \"\" returns isError containing 'непустой content' and sends ZERO requests to GitLab: the guard fires before the GET pre-read (D-10, RESEARCH open question 2)"
    - "move with empty content stays legal (no content key in the body) and delete with content stays an error (D-10)"
    - "ActionIn.Content stays a plain string with json:\"content,omitempty\"; tools/list has no null/anyOf/$ref and the schema guard plus golden snapshot are green (D-10, D-15, FND-08)"
    - "The commit_files and create_or_update_file descriptions state that an empty file cannot be created (pass one newline instead) and stay <= 900 characters; the create_or_update_file concurrency claim is not strengthened (D-10, D-11)"
    - "ROADMAP backlog 999.1 is marked CLOSED in Phase 4 with the shipped approach (string + reject, no pointer) (D-10)"
  artifacts:
    - path: "internal/tools/write.go"
      provides: "empty-content guard in toFileAction (create/update) and createOrUpdateFile"
      contains: "непустой content"
    - path: "internal/tools/write_test.go"
      provides: "no-request table rows for create/update without content; rewritten TestCommitFilesEmptyContentIsSent"
      contains: "непустой content"
    - path: "internal/tools/upsert_test.go"
      provides: "create_or_update_file empty content no-request test"
      contains: "непустой content"
    - path: "internal/tools/testdata/tools_list.golden.json"
      provides: "regenerated tools/list snapshot with new descriptions"
  key_links:
    - from: "internal/tools/write.go toFileAction"
      to: "validateCommitInput / commitCore"
      via: "error returned before any HTTP call"
      pattern: "для %s нужен непустой content"
    - from: "internal/tools/write.go createOrUpdateFile"
      to: "d.GL.RepositoryFiles.GetFile"
      via: "guard placed before the GET"
      pattern: "in\\.Content == \"\""
---

## Phase Goal

**As a** автор агента AiAdventAgentV2, **I want to** прогнать весь сценарий `gitlab-mcp.exe` на реальном gitlab.com и получить готовый `.exe` с README, **so that** я уверен, что сервер работает вживую, и могу подключить его к агенту без догадок.

<objective>
Safety slice before the first real write: close backlog 999.1 (D-10). Today `commit_files` create/update without `content` silently commits an EMPTY file to the real repository, and `create_or_update_file` does the same with `content: ""`. After this plan both tools refuse such calls before any request, the documented rule "empty files are not supported, pass one newline" holds for every write path, and ROADMAP 999.1 is closed.

Purpose: the live run (Plan 04) writes to the real sandbox; a silent destructive write must be impossible first.
Output: guarded write.go, updated descriptions/schema text, hermetic no-request tests, regenerated golden, ROADMAP 999.1 closed.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-live-verification-and-delivery/04-CONTEXT.md
@.planning/phases/04-live-verification-and-delivery/04-RESEARCH.md
@.planning/phases/04-live-verification-and-delivery/04-PATTERNS.md

<interfaces>
From internal/tools/write.go (current):
- `type ActionIn struct { ... Content string \`json:"content,omitempty"\` ... }` (line ~62) — DO NOT change to *string.
- `type fileAction struct { Path, PreviousPath, Content string; sendContent bool; ... }` (comment at L82-84 says an empty string is legitimate content — must be updated).
- `func toFileAction(i int, a ActionIn) (fileAction, error)` — switch at L125-134: `case actDelete` rejects content; `case actCreate, actUpdate: fa.sendContent = true`; `case actMove: fa.sendContent = a.Content != ""`.
- `func validateCommitInput(branch, message string, acts []fileAction) error`.
- `type UpsertFileIn struct { Project, Path, Content, Branch, CommitMessage string }`; `Content` tag: `jsonschema:"full new UTF-8 text content of the file"` (L205).
- `func createOrUpdateFile(d Deps) ...`: NormalizeProject -> NormalizeRepoPath -> `if path == "" { errors.New("не указан путь к файлу") }` -> validateCommitInput -> `d.GL.RepositoryFiles.GetFile(...)`.
- `const commitFilesDescription` (L16) and `const createOrUpdateFileDescription` (L25).

From internal/tools/schema.go L44-47: `"content": {"type": "string", "description": "full new UTF-8 text content; required for create and update, optional for move"}`.

Test helpers (internal/tools, package tools): `newTestSession(t, fake)`, `callText(t, cs, name, args) (text string, isErr bool)`, `commitArgs(actions...)`, `act(action, path)`, `actWith(action, path, key, value)`, `newUpsertFake(t, status, ...)`, `upsertArgs(content string)`, `testutil.NewFakeGitLab(t)`, `fake.Requests()`.
Golden: `go test ./internal/tools -run TestToolsListGolden -update` (flag `-update` in schema_test.go L117).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject empty content on create/update in commit_files and create_or_update_file (999.1)</name>
  <files>internal/tools/write.go, internal/tools/write_test.go, internal/tools/upsert_test.go</files>
  <read_first>
    - internal/tools/write.go (whole file: toFileAction, validateCommitInput, commitCore, createOrUpdateFile, fileAction comment L82-84)
    - internal/tools/write_test.go (TestCommitFilesEmptyContentIsSent L140-168, TestCommitFilesGuardsSendNoRequest L225-273, helper definitions)
    - internal/tools/upsert_test.go (newUpsertFake, upsertArgs L10-31, TestCreateOrUpdateFileGuardsSendNoRequest L208)
    - internal/tools/helpers_test.go (newTestSession, callText)
    - .planning/phases/04-live-verification-and-delivery/04-PATTERNS.md (section "internal/tools/write.go")
  </read_first>
  <behavior>
    - commit_files with actions [{action: create, file_path: a.txt}] (no content key) -> isError, text contains "непустой content", fake.Requests() is empty
    - commit_files with actions [{action: update, file_path: a.txt, content: ""}] -> isError, contains "непустой content", zero requests
    - commit_files with actions [{action: create, file_path: " "}] still fails with a message containing "file_path" (path validation runs before the content check)
    - commit_files move with previous_path and no content -> still one POST, no "content" key in the action body (existing TestCommitFilesSendsOnePostWithAllActions stays green)
    - commit_files delete with content -> still isError containing "delete"
    - create_or_update_file with content "" -> isError, contains "непустой content", zero requests (no GET, no POST)
  </behavior>
  <action>
Per D-10 (backlog 999.1), write the failing tests first, then the guard.

Tests (RED): in write_test.go add two rows to the TestCommitFilesGuardsSendNoRequest table: {"create without content", commitArgs(act("create", "a.txt")), "непустой content"} and {"update with empty content", commitArgs(actWith("update", "a.txt", "content", "")), "непустой content"}. Rewrite TestCommitFilesEmptyContentIsSent (it asserts the OLD behavior) into TestCommitFilesEmptyContentIsRejected: create action with content "" -> isErr true, text contains "непустой content", len(fake.Requests()) == 0. In upsert_test.go add TestCreateOrUpdateFileEmptyContentSendsNoRequest: fake from newUpsertFake (404 status, i.e. "file missing"), call create_or_update_file with upsertArgs(""), assert isErr, text contains "непустой content", len(fake.Requests()) == 0. Run and confirm the new cases fail.

Implementation (GREEN) in write.go:
- In toFileAction split the case: `case actCreate, actUpdate:` first checks `a.Content == ""` and returns `fmt.Errorf("actions[%d]: для %s нужен непустой content (пустой файл создать нельзя: передайте один перевод строки)", i, action)`, otherwise sets `fa.sendContent = true`. Keep actDelete and actMove branches unchanged. Make sure file_path / previous_path validation happens before this switch so the existing "missing file_path" row keeps its message (verify by running the table; reorder only if needed).
- Update the fileAction.sendContent comment: empty content is rejected for create/update; for move an empty content means "keep file content" and is not sent.
- In createOrUpdateFile, directly after the `if path == ""` check and BEFORE validateCommitInput and BEFORE RepositoryFiles.GetFile, add: `if in.Content == ""` return `errors.New("нужен непустой content (пустой файл создать нельзя: передайте один перевод строки)")`.
- Do NOT change ActionIn.Content or UpsertFileIn.Content types (plain string, D-10/D-15: no pointer, schema must not become ["null","string"]).
Error texts are Russian, plain errors (not isError wrappers), matching the style of `errors.New("commit_message не может быть пустым")`.
  </action>
  <verify>
    <automated>go test ./internal/tools -run "TestCommitFiles|TestCreateOrUpdateFile" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "непустой content" internal/tools/write.go` returns >= 2
    - `grep -c "\*string" internal/tools/write.go` returns 0 (no pointer fields introduced)
    - `grep -c "TestCommitFilesEmptyContentIsSent" internal/tools/write_test.go` returns 0 and `grep -c "TestCommitFilesEmptyContentIsRejected" internal/tools/write_test.go` returns 1
    - `grep -c "TestCreateOrUpdateFileEmptyContentSendsNoRequest" internal/tools/upsert_test.go` returns 1
    - `go test ./internal/tools -run "TestCommitFiles|TestCreateOrUpdateFile" -count=1` exits 0
  </acceptance_criteria>
  <done>Both write tools refuse empty content on create/update before any HTTP request; move/delete behavior unchanged; tests prove zero requests.</done>
</task>

<task type="auto">
  <name>Task 2: Document the rule in tool descriptions and schema, regenerate golden, close 999.1 in ROADMAP</name>
  <files>internal/tools/write.go, internal/tools/schema.go, internal/tools/testdata/tools_list.golden.json, .planning/ROADMAP.md</files>
  <read_first>
    - internal/tools/write.go (commitFilesDescription L16-23, createOrUpdateFileDescription L25-31, UpsertFileIn L200-210)
    - internal/tools/schema.go (content property L44-47)
    - internal/tools/schema_test.go (TestToolSchemas, TestToolsListGolden, -update flag)
    - .planning/ROADMAP.md (Backlog 999.1 entry and the closed 999.2 entry format)
  </read_first>
  <action>
Per D-10 and D-11:
- commitFilesDescription: replace the sentence "content — полный новый текст файла." with "content — полный новый текст файла, для create и update обязателен и не может быть пустым (пустой файл создать нельзя: передайте один перевод строки)." Total length must stay <= 900 characters (currently ~553).
- createOrUpdateFileDescription: append before "Для нескольких файлов..." the sentence "content не может быть пустым (пустой файл создать нельзя: передайте один перевод строки)." Do NOT add or strengthen any claim about protection from concurrent edits (999.3 stays open, D-11); leave the existing sentence untouched. Length <= 900.
- UpsertFileIn.Content jsonschema tag: "full new UTF-8 text content of the file; must not be empty (empty files are not supported, pass one newline)".
- schema.go content description: "full new UTF-8 text content; required and non-empty for create and update (empty files are not supported, pass one newline); optional for move". Type stays plain "string".
- Regenerate the golden: `go test ./internal/tools -run TestToolsListGolden -update`, then inspect `git diff internal/tools/testdata/tools_list.golden.json` — only description strings of commit_files / create_or_update_file may change.
- ROADMAP.md backlog: change heading "### Phase 999.1: commit_files: create/update без content не должен коммитить пустой файл (BACKLOG)" suffix to "(CLOSED in Phase 4, plan 04-01)" and add a line after Goal: "**Closed:** реализовано без указателя (D-10): `content` остаётся `string`, пустой `content` при create/update в `commit_files` и в `create_or_update_file` отклоняется до запроса; пустой файл не создаётся (обход: один перевод строки); тесты «нет запроса без content»." Leave 999.3, 999.4, 999.5 unchanged (D-11). Do not touch the Phase 4 plan list section.
  </action>
  <verify>
    <automated>go test ./internal/tools -count=1 && go vet ./internal/tools</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "пустой файл создать нельзя" internal/tools/write.go` returns >= 3 (two descriptions + guard messages)
    - `grep -c "empty files are not supported" internal/tools/schema.go` returns 1
    - `grep -c "пустой файл создать нельзя" internal/tools/testdata/tools_list.golden.json` returns >= 2
    - `grep -c "CLOSED in Phase 4, plan 04-01" .planning/ROADMAP.md` returns 1 and `grep -c "999.3.*(BACKLOG)" .planning/ROADMAP.md` returns 1
    - `go test ./... -count=1` exits 0 (schema guard TestToolSchemas and TestToolsListGolden green; e2e smoke still green)
  </acceptance_criteria>
  <done>Descriptions, schema text and golden describe the empty-content rule; whole suite green; 999.1 closed in ROADMAP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM agent -> MCP tool args | Model-generated arguments (may omit content) reach write handlers |
| MCP server -> gitlab.com REST | Validated arguments become real commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | commit_files toFileAction create/update | mitigate | Reject empty content with Russian error before commitCore; table test asserts len(fake.Requests()) == 0 |
| T-04-02 | Tampering | create_or_update_file | mitigate | Same guard placed before the GET pre-read and the POST; dedicated no-request test |
| T-04-03 | Denial of Service (schema) | tools/list schema | mitigate | No *string; TestToolSchemas (no null/anyOf/$ref) and golden snapshot must stay green |
| T-04-04 | Repudiation / misleading docs | createOrUpdateFileDescription | accept | Concurrency wording left as-is (999.3 deferred per D-11); README (Plan 03) states the real limitation |
</threat_model>

<verification>
- `go test ./... -count=1` passes (no `-race`, D-15)
- `go vet ./...` clean, `gofmt -l internal` prints nothing
</verification>

<success_criteria>
- Empty content never reaches GitLab from either write tool (proved by zero-request tests)
- Schema unchanged in shape; only descriptions differ in the golden
- ROADMAP 999.1 CLOSED; 999.3-999.5 remain BACKLOG
</success_criteria>

<output>
Create `.planning/phases/04-live-verification-and-delivery/04-01-SUMMARY.md` when done
</output>
