---
phase: 04-live-verification-and-delivery
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/build_test.go
  - README.md
autonomous: true
requirements: [QA-04]

must_haves:
  truths:
    - "The canonical build `go build -trimpath -ldflags \"-s -w\" -o gitlab-mcp.exe ./cmd/gitlab-mcp` with CGO_ENABLED=0 produces one .exe that imports only kernel32.dll, reports CGO_ENABLED=0 and -trimpath=true in `go version -m`, and embeds the go-sdk and client-go/v2 modules (module paths asserted, versions not hard-coded); this is asserted by `go test ./...` (D-12, D-13)"
    - "README.md (Russian) shows the build command in both PowerShell form ($env:CGO_ENABLED = \"0\") and bash form, states that gitlab-mcp.exe stays in .gitignore and the agent gets its absolute path (D-12)"
    - "README.md states PAT scopes: api required for commit_files / create_or_update_file / create_branch / MR writes; read_api is enough only for reading; write_repository is not enough (D-14, QA-04)"
    - "README.md shows how to connect the agent: the UI form (Название, Команда = absolute path, Аргументы empty, Переменные окружения GITLAB_TOKEN=... one per line) and the REST JSON body (command with doubled backslashes, \"args\": [], \"env\": {\"GITLAB_TOKEN\": ...}, optional GITLAB_URL) with a placeholder token only (D-14, RESEARCH Code Example 5)"
    - "README.md lists all 20 tools, one row each, and the limitations: Mini scope, unrestricted writes (security by token rights), no empty files (one newline workaround), no delete_branch, no sha merge protection, gitlab.com only, and no protection against someone else's edits between read and write in create_or_update_file (D-10, D-11, D-14)"
    - "README.md explains how to verify: go test ./..., uv run scripts/smoke.py, uv run scripts/live.py --project GROUP/PROJECT with GITLAB_TOKEN and optional GITLAB_TOKEN_READONLY (D-14)"
  artifacts:
    - path: "e2e/build_test.go"
      provides: "TestBinaryHasNoExternalDependencies (canonical build + debug/pe + go version -m)"
      contains: "ImportedSymbols"
    - path: "README.md"
      provides: "Russian build/connect/scope/tools/limitations/verify guide"
      min_lines: 90
      contains: "write_repository"
  key_links:
    - from: "e2e/build_test.go"
      to: "./cmd/gitlab-mcp"
      via: "exec go build -trimpath -ldflags \"-s -w\" with CGO_ENABLED=0"
      pattern: "-trimpath"
    - from: "README.md"
      to: "scripts/live.py CLI"
      via: "documented command and env vars"
      pattern: "scripts/live.py --project"
---

## Phase Goal

**As a** автор агента AiAdventAgentV2, **I want to** прогнать весь сценарий `gitlab-mcp.exe` на реальном gitlab.com и получить готовый `.exe` с README, **so that** я уверен, что сервер работает вживую, и могу подключить его к агенту без догадок.

<objective>
Delivery slice (QA-04): after this plan the author can build the single `gitlab-mcp.exe` with one documented command, `go test ./...` proves it has no external dependencies (only kernel32.dll, no cgo, no Node/npx), and a Russian README tells exactly how to plug it into AiAdventAgentV2 (UI fields and REST JSON), which token scope is needed, what the 20 tools do and what the limits are.

Purpose: QA-04 and ROADMAP Phase 4 criteria 3-4.
Output: e2e/build_test.go, README.md.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-live-verification-and-delivery/04-CONTEXT.md
@.planning/phases/04-live-verification-and-delivery/04-RESEARCH.md
@.planning/phases/04-live-verification-and-delivery/04-PATTERNS.md

<interfaces>
From e2e/main_test.go: `package e2e`; `var binPath string` (built by TestMain without canonical flags — do not reuse for this check); `func envWithout(keys ...string) []string`.
Measured on the current tree (RESEARCH Code Example 4): 11,445,760 bytes; `pe.File.ImportedSymbols()` entries look like "Symbol:kernel32.dll" (48 entries); `ImportedLibraries()` returns an empty list for this exe; `go version -m` shows `build	CGO_ENABLED=0`, `build	-trimpath=true`, deps `github.com/modelcontextprotocol/go-sdk v1.8.0`, `gitlab.com/gitlab-org/api/client-go/v2 v2.64.0` (no -ldflags line is printed — do not assert on it).

live.py CLI (implemented by Plan 02, document exactly this): `uv run scripts/live.py --project GROUP/PROJECT [--exe PATH] [--out PATH]`; env GITLAB_TOKEN (api, required), GITLAB_TOKEN_READONLY (read_api, optional; without it the read-only check is SKIPPED); exit 0/1/2; writes .planning/phases/04-live-verification-and-delivery/LIVE-RUN.md; cleanup deletes only its own live/<ts>-* branches.

Agent (AiAdventAgentV2) facts (RESEARCH F3, Code Example 5, Pitfall 10): servers stored as name/command/args/env/cwd/enabled in its DB; UI fields Название, Команда, Аргументы («по одному в строке»), Переменные окружения («KEY=VALUE, по одной в строке»), Рабочая директория (необязательно), Включён; REST `POST /api/v1/mcp/servers` takes JSON; env values are masked after saving; server name slug <= 20 chars, tools exposed as mcp__<slug>__<tool> (64-char cap; name `gitlab` gives e.g. mcp__gitlab__list_merge_request_notes); call timeout 30 s, result cap 20000 chars.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hermetic no-external-dependencies build test</name>
  <files>e2e/build_test.go</files>
  <read_first>
    - e2e/main_test.go (TestMain build, envWithout, runtime.GOOS usage)
    - e2e/python_smoke_test.go (subprocess + skip patterns)
    - .planning/phases/04-live-verification-and-delivery/04-RESEARCH.md (Code Example 4, Pitfall 9)
  </read_first>
  <behavior>
    - Canonical build into t.TempDir() succeeds with CGO_ENABLED=0
    - Every PE imported symbol's DLL (text after the last ":") lowercased equals "kernel32.dll"; at least one import exists
    - `go version -m <exe>` output contains "CGO_ENABLED=0", "-trimpath=true" and a `dep` line for each module path "github.com/modelcontextprotocol/go-sdk" and "gitlab.com/gitlab-org/api/client-go/v2" (module paths only, no version literals, so a version bump in go.mod does not break the test)
    - File size is logged and is below 25 MB
  </behavior>
  <action>
Per D-12/D-13 create e2e/build_test.go (package e2e) with TestBinaryHasNoExternalDependencies. Skip with t.Skip when runtime.GOOS != "windows" (PE check is Windows-only). Build: exec.Command("go", "build", "-trimpath", "-ldflags", "-s -w", "-o", out, "./cmd/gitlab-mcp") where out = filepath.Join(t.TempDir(), "gitlab-mcp.exe"); cmd.Dir = ".."; cmd.Env = append(envWithout("CGO_ENABLED"), "CGO_ENABLED=0"); fail with combined output on error. Inspect with debug/pe: pe.Open(out), defer Close, ImportedSymbols() (NOT ImportedLibraries(), which is empty for this exe); for each entry take the substring after the last ":" lowercased; t.Errorf on anything other than "kernel32.dll"; t.Fatal when the list is empty. Run `go version -m out` via exec and assert the four substrings from <behavior>: the two build settings plus the two module paths only. Do NOT hard-code version strings such as v1.8.0 / v2.64.0 (brittle on dependency bumps; go.mod is the single source of truth for versions). os.Stat size: t.Logf("size %d bytes") and t.Errorf if >= 25<<20. Do not execute the built exe (Windows would lock the temp dir). Reuse envWithout from main_test.go; do not redefine it. No -race (D-15).
  </action>
  <verify>
    <automated>go test ./e2e -run TestBinaryHasNoExternalDependencies -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./e2e -run TestBinaryHasNoExternalDependencies -count=1 -v` exits 0 and the log contains "size "
    - `grep -c "ImportedSymbols" e2e/build_test.go` returns 1 and `grep -c "func envWithout" e2e/build_test.go` returns 0
    - `grep -c "CGO_ENABLED=0" e2e/build_test.go` >= 1 and `grep -c "\-trimpath" e2e/build_test.go` >= 1
  </acceptance_criteria>
  <done>`go test ./...` proves the canonical build is a single exe with only kernel32.dll imports, no cgo, and the go-sdk and client-go/v2 modules embedded (checked by module path).</done>
</task>

<task type="auto">
  <name>Task 2: Russian README with build, token scopes, agent connection, tools, limits, verification</name>
  <files>README.md</files>
  <read_first>
    - internal/tools/register.go (the 20 tool registrations and descriptions)
    - scripts/smoke.py (EXPECTED_TOOLS L41-62, how it is run)
    - internal/config/config.go (GITLAB_TOKEN, GITLAB_URL default)
    - internal/tools/write.go (createOrUpdateFileDescription — README must NOT repeat its "Защищает от перезаписи чужих правок" claim, D-11)
    - .planning/phases/04-live-verification-and-delivery/04-RESEARCH.md (Code Example 5, Pitfalls 9-10)
    - .gitignore
  </read_first>
  <action>
Create README.md at the repo root, in Russian, compact (per D-14; wording is Claude's discretion). Sections in this order:
1 "GitLabMcpMini" — what it is: stdio MCP server for gitlab.com, 20 tools, for AiAdventAgentV2 (Python mcp 1.30.x); one native exe, no Node/npx.
2 "Сборка" (D-12): Go 1.25+; PowerShell form: `$env:CGO_ENABLED = "0"` then `go build -trimpath -ldflags "-s -w" -o gitlab-mcp.exe ./cmd/gitlab-mcp`; bash form: `CGO_ENABLED=0 go build -trimpath -ldflags "-s -w" -o gitlab-mcp.exe ./cmd/gitlab-mcp`; note that the bash form is a parse error in PowerShell (RESEARCH Pitfall 9); result is one file (~11 MB) that only depends on kernel32.dll (checked by `go test ./e2e -run TestBinaryHasNoExternalDependencies`); the exe is in .gitignore, not committed; the agent is given its absolute path.
3 "Токен и scope": Personal Access Token in GITLAB_TOKEN; `api` is required for writes (create_branch, commit_files, create_or_update_file, create_merge_request, create_merge_request_note, update_merge_request, merge_merge_request); `read_api` is enough only for reading; `write_repository` is NOT enough (it covers git push only, not the REST endpoints); without the token the server exits immediately with a message on stderr; the token is never printed; optional GITLAB_URL (default https://gitlab.com; only gitlab.com is supported).
4 "Подключение к AiAdventAgentV2": explain the agent stores servers as fields in its DB (not an mcpServers file). UI form block: Название `gitlab`; Команда `C:\Projects\GitLabMcpMini\gitlab-mcp.exe` (raw path, single backslashes); Аргументы empty; Переменные окружения `GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx` (one KEY=VALUE per line; add `GITLAB_URL=...` only if needed); Рабочая директория empty; Включён. Then the REST JSON body for `POST /api/v1/mcp/servers` with "name": "gitlab", "command": "C:\\Projects\\GitLabMcpMini\\gitlab-mcp.exe" (doubled backslashes in JSON, or forward slashes), "args": [], "env": {"GITLAB_TOKEN": "glpat-xxxxxxxxxxxxxxxxxxxx"}, "cwd": null, "enabled": true. Notes: env values are masked in the UI after saving; tools appear as mcp__gitlab__<tool>; agent limits (30 s per call, 20000 chars per result — the server truncates with an explicit mark). Placeholder token only, never a real one.
5 "Инструменты": markdown table with 20 rows (name | what it does, one line each, read/write marker): whoami, list_projects, get_project, list_repository_tree, get_file_contents, list_branches, create_branch, list_commits, get_commit, compare_refs, commit_files, create_or_update_file, list_merge_requests, get_merge_request, get_merge_request_diffs, list_merge_request_notes, create_merge_request, create_merge_request_note, update_merge_request, merge_merge_request. Project may be given as numeric ID or group/subgroup/project.
6 "Ограничения" (D-10, D-11, D-14): Mini set, no other GitLab areas; writes are not restricted by the server (author's decision) — safety comes from token rights, use a separate token/project if needed; write requests are never retried automatically; empty file cannot be created (content must be non-empty: pass one newline); no delete_branch (remove the source branch with merge_merge_request remove_source_branch or in GitLab); merge has no sha protection; create_or_update_file re-reads the file itself right before writing, there is NO protection against someone else's edits made between the agent's own read and the write; only gitlab.com is checked (self-hosted not tested).
7 "Проверка": `go test ./...` (hermetic, no network; do not use -race); `uv run scripts/smoke.py` (read-only against gitlab.com with GITLAB_TOKEN; writes only against a local fake); live run `uv run scripts/live.py --project GROUP/PROJECT` with env GITLAB_TOKEN (api) and optional GITLAB_TOKEN_READONLY (read_api; without it the read-only check is SKIPPED) — writes to the given project in live/<timestamp>-* branches, merges a test MR into the default branch (files under live-run/<timestamp>/ stay there), cleans up its own branches/MRs and writes LIVE-RUN.md; use a dedicated sandbox project. Show PowerShell env syntax `$env:GITLAB_TOKEN = "..."`.
Before writing section 7 check `scripts/smoke.py --help` to state its real flags correctly.
  </action>
  <verify>
    <automated>bash -c 'for t in whoami list_projects get_project list_repository_tree get_file_contents list_branches create_branch list_commits get_commit compare_refs commit_files create_or_update_file list_merge_requests get_merge_request get_merge_request_diffs list_merge_request_notes create_merge_request create_merge_request_note update_merge_request merge_merge_request; do grep -q "| *\`$t\`" README.md || { echo "missing $t"; exit 1; }; done; grep -q "write_repository" README.md && grep -q "read_api" README.md && grep -qF "\$env:CGO_ENABLED = \"0\"" README.md && grep -qF "\"args\": []" README.md && grep -qF "scripts/live.py --project" README.md && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - All 20 tool names appear as table rows (verify loop prints OK)
    - `grep -cF 'go build -trimpath -ldflags "-s -w" -o gitlab-mcp.exe ./cmd/gitlab-mcp' README.md` >= 2 (PowerShell and bash forms)
    - `grep -cF 'C:\\Projects\\GitLabMcpMini\\gitlab-mcp.exe' README.md` >= 1 and `grep -cF 'GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx' README.md` >= 1
    - `grep -cE 'glpat-[A-Za-z0-9_-]{20,}' README.md` equals `grep -cE 'glpat-x{20}' README.md` (only the placeholder token appears)
    - `grep -c "Защищает от перезаписи" README.md` returns 0 and `grep -ci "delete_branch" README.md` >= 1 and `grep -ci "перевод строки" README.md` >= 1
  </acceptance_criteria>
  <done>README lets the author build the exe and connect it to AiAdventAgentV2 without guessing, with correct scopes and honest limitations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> author's agent config | Copy-pasted config carries the real token into the agent DB |
| build toolchain -> exe | Supply chain of the delivered binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Information Disclosure | README.md | mitigate | Placeholder `glpat-xxxxxxxxxxxxxxxxxxxx` only; acceptance grep proves no other glpat- token appears |
| T-04-13 | Elevation of Privilege | README scope guidance | mitigate | Document api vs read_api vs write_repository precisely; recommend read_api for read-only use and a sandbox project for live runs |
| T-04-14 | Repudiation / misleading docs | README limitations | mitigate | State unrestricted writes, no sha merge guard, no concurrent-edit protection in create_or_update_file (D-11) |
| T-04-15 | Tampering | exe dependencies | mitigate | TestBinaryHasNoExternalDependencies asserts kernel32.dll-only imports, CGO_ENABLED=0 and presence of the go-sdk/client-go/v2 module paths (versions pinned by go.mod, not by the test) |
| T-04-16 | Tampering | exe in git | accept | .gitignore keeps *.exe out of the repo; author builds locally (D-12) |
</threat_model>

<verification>
- `go test ./... -count=1` passes
- README verify loop prints OK
</verification>

<success_criteria>
- One command builds the exe; the no-external-deps property is enforced by the test suite
- README covers build, scopes, agent connection (UI + JSON), 20 tools, limitations, verification
</success_criteria>

<output>
Create `.planning/phases/04-live-verification-and-delivery/04-03-SUMMARY.md` when done
</output>
