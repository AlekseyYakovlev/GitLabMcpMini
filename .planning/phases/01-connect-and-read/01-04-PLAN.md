---
phase: 01-connect-and-read
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - internal/tools/format.go
  - internal/tools/format_test.go
  - internal/tools/projects.go
  - internal/tools/projects_test.go
  - internal/tools/register.go
autonomous: true
requirements: [READ-01, READ-02, FND-04, FND-06, FND-08]

must_haves:
  truths:
    - "User calls list_projects and gets one line per project '<id> <path_with_namespace> (<default_branch>) — <description>' for projects they are a member of by default"
    - "list_projects ends with '[page N, per_page M — есть следующая страница: вызовите с page=N+1]' when GitLab sends X-Next-Page, otherwise '[page N, per_page M — последняя страница]'"
    - "User calls get_project with a numeric ID or group/subgroup/project and gets key fields as text, including default branch"
    - "Output longer than 15 000 characters is cut on a line boundary and ends with '[вывод обрезан на 15000 символах; сузьте путь или уменьшите per_page]'"
    - "per_page is clamped to 1..100 (default 20) and page < 1 becomes 1"
  artifacts:
    - path: "internal/tools/format.go"
      provides: "OutputBudget, Budget, ClampPaging, PageFooter, TruncatedFooter, oneLine"
      contains: "const OutputBudget = 15000"
    - path: "internal/tools/projects.go"
      provides: "list_projects and get_project handlers and input structs"
      contains: "Membership"
  key_links:
    - from: "internal/tools/projects.go"
      to: "glclient.NormalizeProject"
      via: "project argument normalisation before GetProject"
      pattern: "glclient\\.NormalizeProject"
    - from: "internal/tools/projects.go"
      to: "PageFooter"
      via: "resp.NextPage / resp.NextLink"
      pattern: "PageFooter\\("
    - from: "internal/tools/register.go"
      to: "list_projects, get_project"
      via: "mcp.AddTool with safe()"
      pattern: "\"list_projects\""
---

## Phase Goal

**As a** автор агента AiAdventAgentV2, **I want to** подключить `gitlab-mcp.exe` по stdio и через него проверять токен и читать мои проекты, дерево файлов и содержимое файлов на gitlab.com, **so that** агент изучает код моих репозиториев стандартным MCP-протоколом без Node/npx и без утечки токена.

<objective>
Vertical slice: the agent can discover which projects it can read (`list_projects`, READ-01) and inspect one project (`get_project`, READ-02), with the shared output budget and pagination footer that every list tool in this and later phases uses (FND-06, D-06, D-07, D-08).

Purpose: after this plan a real user of the agent can ask "which of my GitLab projects exist and what is the default branch of X" and get a compact, paginated answer.
Output: `format.go` (shared), two tools registered, tests through the in-memory MCP session against a fake GitLab.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-connect-and-read/01-CONTEXT.md
@.planning/phases/01-connect-and-read/01-RESEARCH.md
@.planning/phases/01-connect-and-read/01-02-SUMMARY.md
@.planning/phases/01-connect-and-read/01-03-SUMMARY.md

<interfaces>
<!-- Existing -->
- tools: Deps, safe[T], Register, withSubject(subject, err), toToolText; test helpers newTestSession(t, fake), callText(t, cs, name, args), testToken
- glclient.NormalizeProject(s) (string, error)
- testutil.NewFakeGitLab / f.JSON(method, rawPath, status, body, headers) / f.Requests()

<!-- Created here (reused by Plans 05-06 and Phases 2-3) -->
package tools:
- const OutputBudget = 15000
- const TruncatedFooter = "[вывод обрезан на 15000 символах; сузьте путь или уменьшите per_page]"
- func Budget(body string, limit int) (out string, truncated bool)   // rune-based; cut at last '\n' at or before limit when one exists in the kept part, else hard rune cut
- func ClampPaging(page, perPage int) (int, int)                      // page<1 -> 1; perPage<1 -> 20; perPage>100 -> 100
- func PageFooter(page, perPage int, resp *gitlab.Response) string    // next when resp != nil && (resp.NextPage > 0 || resp.NextLink != "")
- func oneLine(s string, max int) string                              // first line, trimmed, max runes with "…"
- type ListProjectsIn struct { Search string `json:"search,omitempty"`; IncludePublic bool `json:"include_public,omitempty"`; Page int `json:"page,omitempty"`; PerPage int `json:"per_page,omitempty"` }
- type ProjectIn struct { Project string `json:"project"` }   // reused by later tools for the project argument description

client-go v2 (compile-verified in RESEARCH):
- c.Projects.ListProjects(&gitlab.ListProjectsOptions{ListOptions: gitlab.ListOptions{Page: int64(p), PerPage: int64(pp)}, Membership: gitlab.Ptr(true), Simple: gitlab.Ptr(true), OrderBy: gitlab.Ptr("last_activity_at"), Search: gitlab.Ptr(q)}, gitlab.WithContext(ctx)) -> ([]*gitlab.Project, *gitlab.Response, error)
- c.Projects.GetProject(project, nil, gitlab.WithContext(ctx)) -> *gitlab.Project{ID, PathWithNamespace, DefaultBranch, EmptyRepo, Archived, Visibility, WebURL, Description, LastActivityAt}
- Response.NextPage (from X-Next-Page), Response.NextLink (from Link rel=next); do NOT use X-Total / TotalItems
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared output budget and pagination footer</name>
  <files>internal/tools/format.go, internal/tools/format_test.go</files>
  <read_first>
    - .planning/phases/01-connect-and-read/01-RESEARCH.md (Pattern 5 output budget and footers)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <behavior>
    - Budget("abc", 10) -> ("abc", false)
    - Budget of 20 lines of 1000 ASCII runes each with limit 15000 -> truncated true, result ends right after a '\n'-delimited line (14 full lines kept, no partial line), rune count <= 15000
    - Budget of a single 20000-rune line with no '\n' -> truncated true, exactly 15000 runes
    - Budget counts runes not bytes: 15000 Cyrillic runes (30000 bytes) with limit 15000 -> not truncated
    - ClampPaging(0,0) -> (1,20); (3,500) -> (3,100); (-1,5) -> (1,5)
    - PageFooter(1,20, resp with NextPage 2) -> "[page 1, per_page 20 — есть следующая страница: вызовите с page=2]"
    - PageFooter(2,20, resp with NextPage 0 and NextLink "") -> "[page 2, per_page 20 — последняя страница]"
    - PageFooter(1,20, resp with NextPage 0 and NextLink "https://x?page=2") -> next-page variant with page=2
    - PageFooter(1,20,nil) -> last-page variant
    - oneLine("first\nsecond", 100) -> "first"; oneLine of 200 runes with max 120 -> 120 runes ending with "…"
  </behavior>
  <action>
    Implement the format.go contract from the interfaces block per D-07/D-08 (footer strings exactly as specified, using the em dash "—"). Budget is rune-based because the agent cuts by Python `len(str)` (RESEARCH A5). Composition rule for all tools (document it in a package comment in format.go): body lines first, then if truncated append `TruncatedFooter` on its own line, then the page footer on its own line; footers are appended after budgeting so 15 000 + footers stays well under the agent's 20 000 cut. Never read `X-Total` (FND-06). Write format_test.go covering every behavior line.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/tools/ -run 'Budget|ClampPaging|PageFooter|OneLine' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/tools/ -count=1` exits 0
    - format.go contains `const OutputBudget = 15000`, `есть следующая страница: вызовите с page=`, `последняя страница`, `[вывод обрезан на 15000 символах; сузьте путь или уменьшите per_page]`
    - `grep -En "X-Total|TotalItems|TotalPages" internal/tools/*.go` outputs nothing
  </acceptance_criteria>
  <done>One shared, tested budget/footer implementation exists for every list tool.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: list_projects and get_project tools end to end</name>
  <files>internal/tools/projects.go, internal/tools/projects_test.go, internal/tools/register.go</files>
  <read_first>
    - internal/tools/register.go, internal/tools/whoami.go, internal/tools/errors.go, internal/tools/helpers_test.go (current patterns)
    - internal/tools/format.go (Task 1)
    - .planning/phases/01-connect-and-read/01-RESEARCH.md ("Tool input structs", "client-go call shapes", Pitfalls 4, 5, 9)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-05, D-06, D-07, D-14, D-15)
  </read_first>
  <behavior>
    - list_projects {} -> request "GET /api/v4/projects?membership=true&order_by=last_activity_at&page=1&per_page=20&simple=true" (assert the query contains membership=true, simple=true, page=1, per_page=20; order of params as client-go emits); fake returns 2 projects and header X-Next-Page: 2 -> text lines "101 group/alpha (main) — Alpha project" and "102 group/sub/beta — " rendering (branch omitted when empty, description omitted when empty), last line is the next-page footer with page=2
    - list_projects {"include_public": true, "search": "alp", "page": 3, "per_page": 500} -> query has no membership param, has search=alp, page=3, per_page=100
    - archived project line ends with " [archived]"
    - 60 projects each with a 400-rune description and per_page 100 -> text contains TruncatedFooter and total runes of the result < 15500
    - get_project {"project": "group/sub/my.proj"} -> request path "/api/v4/projects/group%2Fsub%2Fmy%2Eproj"; text contains lines "id: 7", "path: group/sub/my.proj", "default_branch: main", "visibility: private", "web_url: https://gitlab.com/group/sub/my.proj"
    - get_project {"project": "123"} -> request path "/api/v4/projects/123"
    - get_project on an empty repository (empty_repo true, default_branch "") -> text contains "default_branch: (нет — репозиторий пуст)"
    - get_project {"project": "https://gitlab.com/g/p"} -> isError, no request sent to fake
    - get_project for unknown project (fake 404) -> isError text contains "404" and "проект"
    - get_project {"project": 123} (JSON number) -> isError from schema validation, server keeps working (RESEARCH Pitfall 5: strict schema kept)
  </behavior>
  <action>
    Create `projects.go` (READ-01, READ-02, D-05, D-06, D-15): input structs `ListProjectsIn` and `ProjectIn` exactly as in the interfaces block with `jsonschema` descriptions that never start with `word=` (Pitfall 4): project -> "numeric project ID as a string (\"12345\") or full path group/subgroup/project"; search -> "filter by name or path substring"; include_public -> "also include public projects you are not a member of; default false = only your projects"; page -> "page number, default 1"; per_page -> "items per page, default 20, max 100". Plain types only, no pointers (D-15).

    `listProjects(d)` handler: ClampPaging; options with `Simple: gitlab.Ptr(true)`, `OrderBy: gitlab.Ptr("last_activity_at")`, `Membership: gitlab.Ptr(true)` unless IncludePublic, `Search` only when non-empty; `gitlab.WithContext(ctx)`. Line format (D-06): `<id> <path_with_namespace>` + ` (<default_branch>)` when non-empty (RESEARCH A2: simple=true may omit it) + ` — <oneLine(description,120)>` when non-empty + ` [archived]` when archived. Empty result -> "проекты не найдены". Compose with Budget(OutputBudget) + TruncatedFooter + PageFooter per format.go rule.

    `getProject(d)` handler: `glclient.NormalizeProject` (errors returned as-is, they become text via KindOther), `GetProject`, errors wrapped `withSubject("проект", err)`. Output lines: `id: `, `path: `, `default_branch: ` (or "(нет — репозиторий пуст)" when empty), `visibility: `, `archived: yes|no`, `last_activity: ` (YYYY-MM-DD when non-nil), `web_url: `, `description: ` (oneLine 300, omitted when empty); budget applied.

    Register both in `register.go` via `mcp.AddTool` + `safe(d, ...)` with `Annotations: &mcp.ToolAnnotations{ReadOnlyHint: true}`; descriptions ≤900 chars that tell the model what the tool returns and how to paginate (e.g. list_projects: "Список проектов GitLab, доступных по токену (по умолчанию только те, где вы участник). Одна строка на проект: id, путь, ветка по умолчанию, описание. Для следующей страницы вызовите снова с page из подсказки внизу."). Write projects_test.go using newTestSession/callText and fake routes; assert raw request URIs from `fake.Requests()`. TestToolSchemas from Plan 03 must still pass with the two new tools.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/tools/ -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0 (includes TestToolSchemas over whoami, list_projects, get_project)
    - register.go contains `"list_projects"` and `"get_project"`
    - projects.go contains `glclient.NormalizeProject`, `gitlab.Ptr(true)`, `last_activity_at`, `withSubject("проект"`
    - `grep -En "\*int|\*string|\*bool" internal/tools/projects.go` outputs nothing (no pointer fields in input structs)
    - projects_test.go asserts the request URI `/api/v4/projects/group%2Fsub%2Fmy%2Eproj`
  </acceptance_criteria>
  <done>The agent can list its projects page by page and read a project's key fields, including the default branch, as compact text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM args -> list_projects/get_project | project, search, page, per_page untrusted |
| GitLab project data -> LLM | descriptions are attacker-controllable text (prompt injection surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Denial of service | list_projects output | mitigate | per_page clamp 1..100, 15 000-rune Budget with explicit TruncatedFooter, descriptions cut to 120/300 runes by oneLine |
| T-01-17 | Information disclosure | get_project project arg | mitigate | NormalizeProject rejects URLs; host comes only from GITLAB_URL |
| T-01-18 | Tampering (prompt injection) | project descriptions in output | accept | Read-only tools, data rendered as single trimmed lines with field labels; no auto-chaining in the server; agent-side concern |
| T-01-19 | Information disclosure | error text | mitigate | 404/403 wording via toToolText; safe() redacts final text |
</threat_model>

<verification>
- `go test ./... -count=1` green; `go vet ./...` clean; `gofmt -l cmd internal e2e` empty
</verification>

<success_criteria>
- READ-01, READ-02 delivered with FND-06 pagination/truncation and FND-04 project input
</success_criteria>

<output>
Create `.planning/phases/01-connect-and-read/01-04-SUMMARY.md` when done
</output>
