---
phase: 01-connect-and-read
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/testutil/session.go
  - internal/tools/helpers_test.go
  - internal/tools/schema_test.go
  - internal/tools/safe_test.go
  - e2e/purity_test.go
autonomous: true
requirements: [FND-01, FND-02, FND-03, FND-08, QA-01]

must_haves:
  truths:
    - "Every registered tool passes the schema guard: name <= 30 chars, description <= 900 chars, schema type object, no $ref/$defs/anyOf/oneOf and no \"null\" type"
    - "A handler that panics returns isError with 'внутренняя ошибка: ' and the server keeps answering the next call"
    - "A panic or error message containing the token is redacted to [REDACTED] in the tool result"
    - "Over a full raw stdio session (including a 401 call, an unknown tool and invalid arguments) every stdout line is a JSON-RPC 2.0 object, no line contains \\r, and stdout/stderr never contain the token even at LOG_LEVEL=debug"
    - "After stdin is closed the process exits with code 0 within 2 s and writes no further stdout bytes"
  artifacts:
    - path: "internal/testutil/session.go"
      provides: "Connect(t, srv) in-memory MCP client session helper"
      exports: ["Connect"]
    - path: "internal/tools/helpers_test.go"
      provides: "newTestSession(t, fake) and callText helpers for all tool tests"
      contains: "func newTestSession"
    - path: "internal/tools/schema_test.go"
      provides: "FND-08 guard over ListTools"
      contains: "anyOf"
    - path: "e2e/purity_test.go"
      provides: "raw-pipe stdout purity and EOF exit test"
      contains: "TestStdoutPurity"
  key_links:
    - from: "internal/tools/helpers_test.go"
      to: "tools.Register + glclient.New"
      via: "real server wired to FakeGitLab over mcp.NewInMemoryTransports"
      pattern: "NewInMemoryTransports|testutil\\.Connect"
    - from: "e2e/purity_test.go"
      to: "binPath"
      via: "os/exec raw pipes"
      pattern: "StdinPipe"
---

## Phase Goal

**As a** автор агента AiAdventAgentV2, **I want to** подключить `gitlab-mcp.exe` по stdio и через него проверять токен и читать мои проекты, дерево файлов и содержимое файлов на gitlab.com, **so that** агент изучает код моих репозиториев стандартным MCP-протоколом без Node/npx и без утечки токена.

<objective>
Complete the reusable test harness (QA-01 tiers 2 and 3b) and lock the protocol-hygiene guarantees: an in-memory MCP session helper every tool test uses, the FND-08 schema/name/description guard that automatically covers every tool added later, panic and redaction checks for `safe`, and a raw-pipe stdout purity + EOF test against the real binary.

Purpose: Plans 04-06 and Phases 2-3 add tools; this harness makes each new tool automatically checked for schema flatness, limits, stdout purity and token leaks.
Output: test-only files; no production code changes.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-connect-and-read/01-CONTEXT.md
@.planning/phases/01-connect-and-read/01-RESEARCH.md
@.planning/phases/01-connect-and-read/01-01-SUMMARY.md

<interfaces>
<!-- Existing (Plan 01) -->
- tools.Deps{GL *gitlab.Client; Logger *slog.Logger; Redact func(string) string; Timeout time.Duration}; tools.Register(s, d); tools.safe[T](d, h)
- recover text contract: "внутренняя ошибка: " + fmt.Sprint(panicValue), then redacted
- glclient.New(config.Config{Token, BaseURL, LogLevel}, logger); logging.NewRedactor(token); logging.New(w, level, redact)
- testutil.NewFakeGitLab(t) / f.JSON / f.Requests(); e2e: binPath, envWithout(...), startSession(t, fakeURL)

<!-- Created here -->
package testutil:
- func Connect(t testing.TB, srv *mcp.Server) *mcp.ClientSession   // mcp.NewInMemoryTransports(); srv.Connect(ctx, st, nil); mcp.NewClient(&mcp.Implementation{Name:"test",Version:"1"}, nil).Connect(ctx, ct, nil); t.Cleanup closes both. Imports only mcp + testing (no internal/tools import, avoids cycles)

package tools (test files only):
- const testToken = "glpat-TESTSECRET"
- func newTestDeps(t, fake *testutil.FakeGitLab) Deps          // glclient.New with testToken + fake.URL, logger to io.Discard via logging.New, Redact = logging.NewRedactor(testToken), Timeout = CallTimeout
- func newTestSession(t, fake *testutil.FakeGitLab) *mcp.ClientSession   // mcp.NewServer + Register(s, newTestDeps) + testutil.Connect
- func callText(t, cs *mcp.ClientSession, name string, args map[string]any) (text string, isError bool)   // joins all TextContent; t.Fatal on protocol error; asserts text never contains testToken
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-memory session helper, schema guard and safe() panic/redaction tests</name>
  <files>internal/testutil/session.go, internal/tools/helpers_test.go, internal/tools/schema_test.go, internal/tools/safe_test.go</files>
  <read_first>
    - internal/tools/safe.go, internal/tools/register.go, internal/tools/whoami.go (Plan 01)
    - internal/testutil/fakegitlab.go
    - .planning/phases/01-connect-and-read/01-RESEARCH.md ("In-memory session helper and schema guard", Pitfalls 3 and 4)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-04, D-05, D-15)
  </read_first>
  <action>
    Create `internal/testutil/session.go` with `Connect` per the interfaces block. Create `internal/tools/helpers_test.go` (package tools) with `testToken`, `newTestDeps`, `newTestSession`, `callText` per the interfaces block; these are the shared helpers Plans 04-06 use for every tool test, so keep the signatures exact.

    `schema_test.go` (FND-08, D-05, D-15): `TestToolSchemas` builds a session with `newTestSession`, calls `cs.ListTools`, requires at least one tool, and for every tool asserts: `len(Name) <= 30`; Name matches `^[a-z][a-z0-9_]*$` and does not start with `gitlab_` (D-05, no prefix); `utf8.RuneCountInString(Description)` is > 0 and <= 900 (characters, not bytes: this is the single authoritative measure for the ROADMAP "≤900 chars" limit and matches Python `len(str)` in scripts/smoke.py; Cyrillic text is 2 bytes per character, so a byte check would disagree); marshalled `InputSchema` has `"type":"object"`; a recursive walk over the decoded JSON fails on any key `$ref`, `$defs`, `anyOf`, `oneOf`, `allOf` and on a `type` value that is an array (which is how pointer fields render `["null", ...]`); every property has a non-empty `description`. The test iterates whatever tools are registered, so it automatically covers tools added by later plans.

    `safe_test.go`: `TestSafeRecoversPanic` registers on a fresh `mcp.NewServer` a test tool `boom` with `mcp.AddTool(s, &mcp.Tool{Name:"boom", Description:"test"}, safe(deps, func(ctx context.Context, in struct{}) (string, error) { panic("kaboom " + testToken) }))` (write it as normal Go in the test file), plus a second tool `ok` returning "fine"; via `testutil.Connect` call `boom` -> isError true, text starts with "внутренняя ошибка: ", contains "[REDACTED]", does not contain testToken; then call `ok` -> "fine" (server survived). `TestSafeRedactsErrorText`: a tool returning `errors.New("bad " + testToken)` -> isError, text contains "[REDACTED]" and not testToken. Do not assert exact wording of error mapping (owned by Plan 02).
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/tools/ -run 'TestToolSchemas|TestSafe' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/tools/ -count=1` exits 0
    - `internal/tools/schema_test.go` contains `$ref`, `$defs`, `anyOf`, `oneOf`, `900`, `30`, `utf8.RuneCountInString`
    - `internal/tools/safe_test.go` contains `TestSafeRecoversPanic` and `внутренняя ошибка: `
    - `grep -c "internal/tools" internal/testutil/session.go` outputs 0 (no import cycle)
  </acceptance_criteria>
  <done>Tool tests have a one-line session helper; schema limits, flatness, no-prefix naming, panic recovery and redaction are enforced for all current and future tools.</done>
</task>

<task type="auto">
  <name>Task 2: Raw-pipe stdout purity, token-leak and EOF-exit test on the real binary</name>
  <files>e2e/purity_test.go</files>
  <read_first>
    - e2e/main_test.go, e2e/stdio_test.go (binPath, envWithout, FakeGitLab setup from Plan 01)
    - .planning/phases/01-connect-and-read/01-RESEARCH.md ("Raw purity check", Tier 3b row, Pitfalls 10 and 11; note: keep stdin open until responses are read)
  </read_first>
  <action>
    `TestStdoutPurity` (FND-02, FND-03, FND-01): start a FakeGitLab where `GET /api/v4/user` returns 401 `{"message":"401 Unauthorized"}`. Start `binPath` with `os/exec`, env `envWithout("GITLAB_TOKEN","GITLAB_URL","LOG_LEVEL")` + `GITLAB_TOKEN=glpat-TESTSECRET`, `GITLAB_URL=<fake.URL>`, `LOG_LEVEL=debug` (maximises log volume); use `StdinPipe`, `StdoutPipe`, and capture stderr in a mutex-guarded buffer. Read stdout with a `bufio.Reader` on raw bytes (`ReadBytes('\n')`) in a goroutine feeding a channel. Send newline-terminated JSON lines and wait for each response by id (timeout 10 s each): id 1 `initialize` (protocolVersion "2025-11-25", capabilities {}, clientInfo {name "purity", version "1"}); notification `notifications/initialized`; assert `len(fake.Requests()) == 0` at this point; id 2 `tools/list`; assert still 0 requests; id 3 `tools/call` whoami (-> result.isError true and the text contains "401"; do not assert exact wording, which Plan 02 owns); id 4 `tools/call` name "no_such_tool"; id 5 `tools/call` whoami with arguments `{"bogus":1}`. For every stdout line: no `\r` byte, `json.Unmarshal` into a map succeeds, `jsonrpc == "2.0"`, and it has `id` plus `result` or `error` (or is a notification with `method`). Then close stdin and assert `cmd.Wait()` returns exit code 0 within 2 s and the reader goroutine yields EOF with no extra bytes. Finally assert neither the collected stdout bytes nor stderr contain `glpat-TESTSECRET`.

    Add `TestMissingTokenStdoutEmpty` only if TestStdioMissingToken in stdio_test.go does not already assert empty stdout; otherwise skip this addition (do not duplicate). Use `t.Cleanup` to kill the process if a step times out so Windows temp cleanup never hits a locked exe.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./e2e/ -run TestStdoutPurity -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./e2e/ -count=1` exits 0
    - `e2e/purity_test.go` contains `LOG_LEVEL=debug`, `glpat-TESTSECRET`, `"2.0"`, `\r`, `no_such_tool`
    - The test asserts `len(fake.Requests()) == 0` after initialize and after tools/list
  </acceptance_criteria>
  <done>Stdout purity, zero startup network I/O, token absence at debug log level, and clean exit on stdin EOF are locked by a raw-byte test on the real binary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> stdout/stderr | protocol stream and logs visible to the agent (stderr tail shown on connect errors) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Information disclosure | stderr logs at debug | mitigate | Purity test runs with LOG_LEVEL=debug and fails if glpat-TESTSECRET appears in stdout or stderr |
| T-01-13 | Tampering (protocol) | any dependency writing to stdout | mitigate | Raw-byte line check: every line valid JSON-RPC 2.0, no CR; includes error paths |
| T-01-14 | Denial of service | handler panic | mitigate | TestSafeRecoversPanic proves the server answers the next call after a panic |
| T-01-15 | Tampering | schema drift (pointer fields, $ref) | mitigate | TestToolSchemas walks every registered tool's schema; runs for all future tools |
</threat_model>

<verification>
- `go test ./... -count=1` green
</verification>

<success_criteria>
- QA-01 tiers 2 and 3b in place and reusable; FND-02, FND-03, FND-08 enforced by tests
</success_criteria>

<output>
Create `.planning/phases/01-connect-and-read/01-03-SUMMARY.md` when done
</output>
