---
phase: 01-connect-and-read
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - go.mod
  - go.sum
  - internal/glclient/client.go
  - internal/glclient/retry.go
  - internal/glclient/transport.go
  - internal/glclient/client_test.go
  - internal/glclient/errors.go
  - internal/glclient/errors_test.go
  - internal/glclient/normalize.go
  - internal/glclient/normalize_test.go
  - internal/glclient/wire_test.go
  - internal/tools/errors.go
  - internal/tools/errors_test.go
  - internal/tools/safe.go
  - internal/tools/whoami.go
autonomous: true
requirements: [FND-04, FND-05, FND-07, QA-01]

must_haves:
  truths:
    - "A failing GET (503, or 429 with short Retry-After) is retried at most 2 times; POST/PUT/DELETE are sent exactly once whatever the status"
    - "A 429 with Retry-After longer than 5 s is not slept on; the tool text says to retry in N s"
    - "A call against a slow GitLab ends at the context deadline with text 'Превышено время ожидания (25 с)' instead of hanging"
    - "401/403/404/429/5xx/400-class/network errors reach the agent as isError with short Russian messages; HTML error bodies are never echoed"
    - "Project path group/sub/my.proj, numeric 123, Cyrillic, space, #, +, % and file paths are sent on the wire exactly as in the RESEARCH wire table (no double encoding)"
    - "A response body over the cap fails with a 'слишком большой' message instead of exhausting memory"
  artifacts:
    - path: "internal/glclient/retry.go"
      provides: "checkRetry (GET/HEAD only), backoff honouring Retry-After, noLimiter"
      contains: "func checkRetry"
    - path: "internal/glclient/errors.go"
      provides: "Classify(err) *Error with Kind, Status, RetryAfter, Detail"
      exports: ["Classify", "Error", "Kind"]
    - path: "internal/glclient/normalize.go"
      provides: "NormalizeProject, NormalizeRepoPath"
      exports: ["NormalizeProject", "NormalizeRepoPath"]
    - path: "internal/tools/errors.go"
      provides: "toToolText(err), withSubject(subject, err)"
      contains: "func toToolText"
  key_links:
    - from: "internal/glclient/client.go"
      to: "retry.go policy"
      via: "gitlab.WithCustomRetry + WithCustomBackoff + WithCustomRetryMax(2) + WithCustomLimiter"
      pattern: "WithCustomRetryMax\\(2\\)"
    - from: "internal/tools/safe.go"
      to: "toToolText"
      via: "errorText delegates to toToolText"
      pattern: "toToolText"
    - from: "internal/tools/errors.go"
      to: "glclient.Classify"
      via: "classification before wording"
      pattern: "glclient\\.Classify"
---

## Phase Goal

**As a** автор агента AiAdventAgentV2, **I want to** подключить `gitlab-mcp.exe` по stdio и через него проверять токен и читать мои проекты, дерево файлов и содержимое файлов на gitlab.com, **so that** агент изучает код моих репозиториев стандартным MCP-протоколом без Node/npx и без утечки токена.

<objective>
Make the skeleton's GitLab path safe and predictable: GET-only retries with Retry-After backoff and a 10 s total wait cap (D-02), zero retries for writes (D-03, FND-07), timeouts and a response body cap, readable error mapping for every failure class (FND-05, D-15), and the single project/path normaliser verified against the wire (FND-04, D-14).

Purpose: after this plan, the agent calling `whoami` with a bad token, a rate limit, a GitLab outage or a network failure gets a clear `isError` message instead of a raw client-go string, a hang, or a crash; and Phases 2-4 inherit a write-safe client.
Output: hardened `internal/glclient`, tool-layer error wording, Tier-1 client tests.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-connect-and-read/01-CONTEXT.md
@.planning/phases/01-connect-and-read/01-RESEARCH.md
@.planning/phases/01-connect-and-read/01-01-SUMMARY.md

<interfaces>
<!-- Existing (from Plan 01) -->
- glclient.New(cfg config.Config, logger *slog.Logger) (*gitlab.Client, error) with helpers newHTTPClient(), clientOptions()
- tools.Deps{GL, Logger, Redact, Timeout}; tools.safe[T]; tools.errorText(err) string; recover text prefix "внутренняя ошибка: " (DO NOT change: Plan 03 tests it)
- testutil.NewFakeGitLab(t), f.Handle, f.JSON, f.Requests(), f.URL

<!-- Created here (consumed by Plans 04-06 and Phases 2-3) -->
package glclient:
- var ErrBodyTooLarge = errors.New("response body too large")
- type limits struct { maxBody int64 }; const defaultMaxBody = 8 << 20; func newClient(cfg, logger, l limits) (*gitlab.Client, error); New calls newClient(cfg, logger, limits{maxBody: defaultMaxBody})
- func checkRetry(ctx context.Context, resp *http.Response, err error) (bool, error)
- func backoff(min, max time.Duration, attempt int, resp *http.Response) time.Duration
- type noLimiter struct{}; func (noLimiter) Wait(context.Context) error { return nil }
- type Kind int; const ( KindOther Kind = iota; KindUnauthorized; KindForbidden; KindNotFound; KindRateLimited; KindServer; KindBadRequest; KindNetwork; KindTimeout; KindCanceled; KindTooLarge; KindDecode )
- type Error struct { Kind Kind; Status int; RetryAfter time.Duration; Detail string }; func (e *Error) Error() string
- func Classify(err error) *Error
- var ErrProjectURL, ErrEmptyProject, ErrDotDot error (Russian text, see Task 3)
- func NormalizeProject(s string) (string, error); func NormalizeRepoPath(p string) (string, error)

package tools:
- func withSubject(subject string, err error) error   // subject e.g. "проект", "файл", "путь или ref в дереве"
- func toToolText(err error) string

client-go v2 facts (verified in RESEARCH): 404 is the shared sentinel gitlab.ErrNotFound (errors.Is), other HTTP errors are *gitlab.ErrorResponse with field Response *http.Response (Retry-After in Response.Header); retryablehttp types: retryablehttp.CheckRetry, retryablehttp.Backoff; options gitlab.WithCustomRetry, gitlab.WithCustomBackoff, gitlab.WithCustomRetryMax, gitlab.WithCustomLimiter; raw requests for tests: c.NewRequest(method, "test", nil, nil) + c.Do(req, nil).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET-only retry policy, Retry-After backoff, no-op limiter, body cap</name>
  <files>go.mod, go.sum, internal/glclient/client.go, internal/glclient/retry.go, internal/glclient/transport.go, internal/glclient/client_test.go</files>
  <read_first>
    - internal/glclient/client.go (current Plan 01 construction)
    - .planning/phases/01-connect-and-read/01-RESEARCH.md (Pattern 1 incl. checkRetry/backoff and budget arithmetic, Pattern 6 capBody, Pitfalls 1 and 2)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-02, D-03)
  </read_first>
  <behavior>
    - GET -> 503 always: 3 attempts total (fake counts), elapsed < 3 s
    - GET -> 429 with `Retry-After: 1`: 3 attempts, elapsed between 1.9 s and 4 s
    - GET -> 429 with `Retry-After: 30`: exactly 1 attempt, returns promptly (< 1 s)
    - GET -> 404: 1 attempt; GET -> 501: 1 attempt
    - POST -> 500, POST -> 429 (Retry-After: 1), PUT -> 503, DELETE -> 503: exactly 1 attempt each
    - GET with `RateLimit-Reset` 50 s in the future and `RateLimit-Limit: 1` headers on success responses: two consecutive GETs both finish in < 1 s (no built-in limiter / RateLimit-Reset sleeping)
    - GET against a handler sleeping 2 s with ctx timeout 300 ms: error satisfies errors.Is(err, context.DeadlineExceeded) and elapsed < 1 s
    - GET returning a 3 MiB JSON body via newClient(..., limits{maxBody: 1 << 20}): errors.Is(err, ErrBodyTooLarge)
    - GET to a closed listener (connection refused): returns an error (no panic), elapsed < 5 s
  </behavior>
  <action>
    Per D-02/D-03 and RESEARCH Pattern 1: run `go get github.com/hashicorp/go-retryablehttp@v0.7.8` (already a client-go dependency, approved in the Package Legitimacy Audit; needed only for the callback types). In `retry.go` implement `checkRetry` exactly with the semantics of RESEARCH Pattern 1: stop on ctx error; on transport error retry only when the `*url.Error` Op is "Get" or "Head" and it is not a DNS not-found; on responses retry only when `resp.Request.Method` is GET or HEAD and (status 429 with Retry-After <= 5 s and remaining deadline > Retry-After + 1 s) or (status >= 500 and != 501); everything else false. Helper `retryAfter(resp) time.Duration` parses integer seconds from `Retry-After` (0 when absent/unparseable). `backoff`: for 429 return retryAfter or 1 s when 0, capped at 5 s; otherwise 500 ms. `noLimiter` per interfaces. Worst case waiting = 2 x 5 s = 10 s (D-02).

    In `transport.go` implement `capRT` + `capBody` from RESEARCH Pattern 6 with exported sentinel `ErrBodyTooLarge`.

    In `client.go` refactor to `newClient(cfg, logger, limits)`; wrap the transport with `capRT{rt, l.maxBody}`; add options `gitlab.WithCustomRetry(retryablehttp.CheckRetry(checkRetry))`, `gitlab.WithCustomBackoff(backoff)`, `gitlab.WithCustomRetryMax(2)`, `gitlab.WithCustomLimiter(noLimiter{})`. Keep `New` signature unchanged. Do NOT use `WithoutRetries()` or `WithOnlyIdempotentRetries()` (both wrong for D-02/D-03, see RESEARCH Alternatives). Add a doc comment on `New` stating writes are never retried (FND-07) so Phase 2 authors see it.

    `client_test.go`: table-driven tests for every behavior line using `testutil.NewFakeGitLab` (count attempts with `len(f.Requests())`) and `c.NewRequest(method, "test", nil, nil)` + `c.Do(req, nil)` for POST/PUT/DELETE; GETs may use the same raw request path. Use `gitlab.WithContext(ctx)` as the request option for the deadline case.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/glclient/ -run 'Retry|Backoff|Limiter|Deadline|BodyCap|Refused' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/glclient/ -count=1` exits 0
    - `internal/glclient/client.go` contains `WithCustomRetryMax(2)`, `WithCustomRetry(`, `WithCustomBackoff(`, `WithCustomLimiter(noLimiter{})`
    - `grep -En "WithoutRetries|WithOnlyIdempotentRetries" internal/glclient/*.go | grep -v _test.go` outputs nothing
    - go.mod lists `github.com/hashicorp/go-retryablehttp v0.7.8` as a direct requirement
    - Test output shows POST/PUT/DELETE cases asserting exactly 1 attempt
  </acceptance_criteria>
  <done>client-go retries only reads (<=2 retries, <=10 s waiting), never writes, never blocks on its own limiter, and cannot read an unbounded body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Error classification and readable isError wording (whoami gets it end-to-end)</name>
  <files>internal/glclient/errors.go, internal/glclient/errors_test.go, internal/tools/errors.go, internal/tools/errors_test.go, internal/tools/safe.go, internal/tools/whoami.go</files>
  <read_first>
    - internal/tools/safe.go, internal/tools/whoami.go (Plan 01)
    - .planning/phases/01-connect-and-read/01-RESEARCH.md (Pattern 7 error table, Pitfalls 6 and 7, Pattern 5 last bullet on capped error bodies)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-02 message "повторите через N с", D-04, D-15)
  </read_first>
  <behavior>
    - Classify via real client-go calls against FakeGitLab: 401 -> KindUnauthorized; 403 `{"error":"insufficient_scope"}` -> KindForbidden; 404 -> KindNotFound (errors.Is gitlab.ErrNotFound); 429 + Retry-After: 42 -> KindRateLimited, RetryAfter 42 s; 502 with body `<html>bad gateway</html>` -> KindServer, Status 502; 400 `{"message":{"base":["a","b"]}}` -> KindBadRequest; 405/409/422 -> KindBadRequest with Status set; closed listener -> KindNetwork; context.DeadlineExceeded -> KindTimeout; context.Canceled -> KindCanceled; wrapped ErrBodyTooLarge -> KindTooLarge; *json.SyntaxError -> KindDecode; errors.New("x") -> KindOther, Detail "x"
    - toToolText: KindUnauthorized -> starts with "401: GitLab отклонил токен"; KindForbidden -> starts with "403: недостаточно прав"; KindNotFound with withSubject("файл", err) -> contains "404" and "файл"; without subject -> contains "проект, путь или ref"; KindRateLimited 42 s -> contains "повторите через 42 с"; KindRateLimited with RetryAfter 0 -> "повторите через 60 с"; KindServer 502 -> "502: ошибка сервера GitLab, повторите позже" and does NOT contain "<html"; KindBadRequest -> "<status>: GitLab отклонил запрос: <detail>" with detail capped at 300 runes; KindTimeout -> "Превышено время ожидания (25 с)"; KindNetwork -> starts with "Не удалось связаться с GitLab"; KindTooLarge -> contains "слишком большой"; KindOther -> detail text (so normaliser errors pass through)
    - A deadline through safe(): Deps.Timeout 200 ms, handler blocks on ctx.Done() and returns ctx.Err() -> result IsError=true with text "Превышено время ожидания (25 с)" within 1 s (use a direct call of the handler returned by safe with a nil *mcp.CallToolRequest)
  </behavior>
  <action>
    Implement `glclient.Classify` per the interfaces block and RESEARCH Pattern 7 (per D-15): check order ctx.Canceled, ctx.DeadlineExceeded, ErrBodyTooLarge, gitlab.ErrNotFound (never mutate the sentinel), `*gitlab.ErrorResponse` (status from `.Response.StatusCode`, Retry-After from `.Response.Header`, Detail = `.Message` capped at 300 runes), `*json.SyntaxError`/`*json.UnmarshalTypeError`, `*url.Error`/`net.Error` -> KindNetwork (Detail = cause string capped), otherwise KindOther. Status mapping: 401, 403, 404, 429, >=500 -> dedicated kinds; 400/405/409/422 and other 4xx -> KindBadRequest. For KindServer set Detail to empty (never echo HTML bodies, Pitfall 7). Nil err -> nil.

    `internal/tools/errors.go`: `withSubject` wraps into an unexported `subjectError{subject string; err error}` with `Unwrap`. `toToolText(err)` extracts subject via errors.As, calls `glclient.Classify`, returns the Russian messages from the behavior block, using exactly: "401: GitLab отклонил токен (недействителен, просрочен или отозван). Проверьте GITLAB_TOKEN."; "403: недостаточно прав: у токена нет нужного scope (read_api/api) или ваша роль в проекте не позволяет это действие."; "404: не найдено (<subject or 'проект, путь или ref'>). GitLab также отвечает 404, если токен не видит приватный проект."; "429: превышен лимит запросов GitLab, повторите через N с." (D-02); "<status>: ошибка сервера GitLab, повторите позже."; "<status>: GitLab отклонил запрос: <detail>"; "Не удалось связаться с GitLab: <detail>"; "Превышено время ожидания (25 с)."; "Вызов отменён."; "Ответ GitLab слишком большой (больше 8 МБ) — объект слишком велик для чтения через API."; "Неожиданный ответ GitLab (не удалось разобрать JSON)."; KindOther -> Detail.

    In `safe.go` change only the body of `errorText` to `return toToolText(err)` and log non-nil errors at Warn with the redacted text (logger already redacts). Keep the recover branch text unchanged. In `whoami.go` wrap the CurrentUser error with `withSubject("пользователь токена", err)`.

    Tests: `glclient/errors_test.go` drives real client-go calls (e.g. `c.Users.CurrentUser`) against FakeGitLab routes for each status; `tools/errors_test.go` is table-driven over `toToolText` with constructed `*glclient.Error` values wrapped as needed plus the safe() deadline case.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/glclient/ ./internal/tools/ -count=1 && go test ./e2e/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/glclient/ ./internal/tools/ ./e2e/ -count=1` exits 0
    - `internal/tools/errors.go` contains `glclient.Classify`, `повторите через`, `Превышено время ожидания (25 с)`, `GitLab отклонил токен`
    - `internal/tools/safe.go` contains `toToolText` and still contains `внутренняя ошибка: `
    - A test asserts the 502 HTML body text `<html` is absent from toToolText output
  </acceptance_criteria>
  <done>Every GitLab/network/timeout failure becomes one short Russian isError message, and whoami demonstrates it end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Project and repository path normalisers verified on the wire</name>
  <files>internal/glclient/normalize.go, internal/glclient/normalize_test.go, internal/glclient/wire_test.go</files>
  <read_first>
    - .planning/phases/01-connect-and-read/01-RESEARCH.md (Pattern 4 normalisers and the full "Verified wire encoding" table; Security Domain V5/V9)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-14)
    - internal/testutil/fakegitlab.go (Requests() returns raw RequestURI)
  </read_first>
  <behavior>
    - NormalizeProject: " group/sub/proj/ " -> "group/sub/proj"; "123" -> "123"; "group%2Fproj" -> "group/proj"; "https://gitlab.com/g/p" -> ErrProjectURL; "" and "/" -> ErrEmptyProject
    - NormalizeRepoPath: `src\main.go` -> "src/main.go"; "./a/b" -> "a/b"; "/a" -> "a"; "a/../b" -> ErrDotDot; "" -> "" (root)
    - Wire (fake records RequestURI; project g/p unless noted): GetProject("group/sub/my.proj") -> "/api/v4/projects/group%2Fsub%2Fmy%2Eproj"; GetProject("123") -> "/api/v4/projects/123"; GetProject("gr oup/проект") -> "/api/v4/projects/gr%20oup%2F%D0%BF%D1%80%D0%BE%D0%B5%D0%BA%D1%82"; GetProject("a#b/c+d/e%f" passed directly, not through NormalizeProject) -> "/api/v4/projects/a%23b%2Fc+d%2Fe%25f"; GetFile path "src/main.go" ref "main" -> path ".../repository/files/src%2Fmain%2Ego"; "dir with space/a#b.txt" -> "dir%20with%20space%2Fa%23b%2Etxt"; "a+b.txt" -> "a+b%2Etxt"; "100%.txt" -> "100%25%2Etxt"; "файл/имя.go" -> "%D1%84%D0%B0%D0%B9%D0%BB%2F%D0%B8%D0%BC%D1%8F%2Ego"; ".gitignore" -> "%2Egitignore"; ref "feature/x y+z#1" -> query "ref=feature%2Fx+y%2Bz%231"; ListTree on "g/sub/p" path "dir with space/ф+#%" ref "feature/x" page 2 per_page 20 recursive -> "/api/v4/projects/g%2Fsub%2Fp/repository/tree?page=2&path=dir+with+space%2F%D1%84%2B%23%25&per_page=20&recursive=true&ref=feature%2Fx"
    - Normalised inputs are never pre-encoded: NormalizeProject("group/sub/my.proj") passed to GetProject yields exactly one level of encoding (no "%25" before "2F")
  </behavior>
  <action>
    Implement `normalize.go` per D-14 and RESEARCH Pattern 4: `NormalizeProject` trims space, rejects `http://`/`https://` prefixes with `ErrProjectURL` ("укажите проект числовым ID или путём group/subgroup/project, а не URL"), unescapes via `url.PathUnescape` when the input contains `%` and unescaping succeeds, trims `/`, rejects empty with `ErrEmptyProject` ("не указан проект"). `NormalizeRepoPath` converts `\` to `/`, trims space, strips leading `./` and `/`, rejects any `..` segment with `ErrDotDot` ("путь не может содержать '..'"). Neither function percent-encodes: client-go encodes once (do not call url.PathEscape, gitlab.PathEscape or url.QueryEscape here).

    `wire_test.go`: table-driven over the full behavior wire table using real client-go calls (`c.Projects.GetProject`, `c.RepositoryFiles.GetFile` with `&gitlab.GetFileOptions{Ref: gitlab.Ptr(ref)}`, `c.Repositories.ListTree` with `ListOptions{Page: 2, PerPage: 20}`, `Path`, `Ref`, `Recursive: gitlab.Ptr(true)`), against a FakeGitLab; unmatched routes return the default 404, which is fine because only the recorded RequestURI is asserted and the returned errors are ignored (404 is never retried, so each case records exactly one request). Assert on the recorded raw RequestURI strings exactly. Also assert every request carries header `Private-Token` and no other header contains the token.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/glclient/ -run 'Normalize|Wire' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/glclient/ -count=1` exits 0
    - `wire_test.go` contains the literal expectations `group%2Fsub%2Fmy%2Eproj`, `%2Egitignore`, `100%25%2Etxt`, `ref=feature%2Fx+y%2Bz%231`
    - `grep -En "PathEscape|QueryEscape" internal/glclient/normalize.go` outputs nothing
  </acceptance_criteria>
  <done>One normaliser for projects and one for repo paths exist, and the wire encoding for subgroup, space, #, +, %, Cyrillic and branch names with / is locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool args -> normalisers | project/path strings are untrusted |
| GitLab responses -> server | error bodies, headers and body size are untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering / Repudiation | glclient.checkRetry | mitigate | POST/PUT/DELETE never retried (duplicate commits/comments impossible); tests assert exactly 1 attempt per write method |
| T-01-08 | Denial of service | glclient transport | mitigate | 8 MiB body cap (ErrBodyTooLarge), 15 s per-attempt timeout, RetryMax 2, Retry-After >5 s surfaced not slept, no-op limiter so RateLimit-Reset cannot stall calls |
| T-01-09 | Information disclosure | tools.toToolText | mitigate | Classified messages only; 5xx bodies dropped; detail capped at 300 runes; final text redacted in safe() |
| T-01-10 | Information disclosure | NormalizeProject | mitigate | URL-shaped project refs rejected, so tool args cannot redirect the token to another host; base URL only from env |
| T-01-11 | Tampering | NormalizeRepoPath | mitigate | `..` segments rejected, `\` normalised, leading `/` and `./` stripped; wire tests lock single encoding |
| T-01-SC | Tampering | go get go-retryablehttp | accept | v0.7.8 is already in client-go's module graph (checksum-verified go.sum) and approved in RESEARCH audit |
</threat_model>

<verification>
- `go test ./... -count=1` green; `go vet ./...` clean
</verification>

<success_criteria>
- FND-05, FND-07, D-02, D-03 proven by attempt-count and message tests
- FND-04 / D-14 proven by exact RequestURI assertions
</success_criteria>

<output>
Create `.planning/phases/01-connect-and-read/01-02-SUMMARY.md` when done
</output>
