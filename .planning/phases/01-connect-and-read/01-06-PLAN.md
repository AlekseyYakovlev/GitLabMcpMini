---
phase: 01-connect-and-read
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - internal/tools/file.go
  - internal/tools/file_test.go
  - internal/tools/register.go
  - e2e/stdio_test.go
  - e2e/python_smoke_test.go
  - scripts/smoke.py
autonomous: true
requirements: [READ-04, FND-04, FND-06, QA-01, QA-02]

must_haves:
  truths:
    - "User calls get_file_contents with project, path and optional ref and gets the decoded text with a header naming path, ref (and '(ветка по умолчанию)' when defaulted), line range, total lines and size"
    - "start_line/end_line return exactly that inclusive 1-based line range"
    - "A file larger than 15 000 characters without a range returns its head plus '[файл обрезан на 15000 символах: показаны строки 1-K из N; используйте start_line/end_line]'"
    - "A binary file returns only 'файл бинарный, N байт, blob_id X' with no content and no base64"
    - "The real binary, driven over stdio, sends project and file paths on the wire exactly once-encoded (subgroup, space, #, +, %, Cyrillic, feature/x ref)"
    - "The Python mcp 1.30.0 smoke script lists all five tools and calls each of them successfully against the fake GitLab; with a real GITLAB_TOKEN and no --base-url it runs read-only live checks against gitlab.com"
  artifacts:
    - path: "internal/tools/file.go"
      provides: "get_file_contents handler, FileIn, isBinary, selectLines"
      contains: "RepositoryFiles.GetFile"
    - path: "e2e/stdio_test.go"
      provides: "TestStdioWirePaths over the real binary"
      contains: "TestStdioWirePaths"
    - path: "scripts/smoke.py"
      provides: "all-tools smoke + optional live read-only mode"
      contains: "get_file_contents"
  key_links:
    - from: "internal/tools/file.go"
      to: "resolveRef"
      via: "ref required by the files endpoint; default branch when empty (D-11)"
      pattern: "resolveRef\\("
    - from: "internal/tools/file.go"
      to: "base64.StdEncoding"
      via: "decode File.Content"
      pattern: "base64\\.StdEncoding"
    - from: "e2e/python_smoke_test.go"
      to: "scripts/smoke.py"
      via: "uv run with --base-url fake"
      pattern: "smoke\\.py"
---

## Phase Goal

**As a** автор агента AiAdventAgentV2, **I want to** подключить `gitlab-mcp.exe` по stdio и через него проверять токен и читать мои проекты, дерево файлов и содержимое файлов на gitlab.com, **so that** агент изучает код моих репозиториев стандартным MCP-протоколом без Node/npx и без утечки токена.

<objective>
Final vertical slice of the phase: the agent can read file contents (`get_file_contents`, READ-04) safely — base64 decode, line ranges, explicit truncation, binary and oversize protection (D-09, D-10, D-11) — and the whole five-tool server is proven through the real binary (wire-path e2e, D-14) and through the agent's own client library (Python smoke, QA-02).

Purpose: completes the phase goal "агент читает проекты, дерево файлов и содержимое файлов" and closes ROADMAP success criteria 3 and 5.
Output: `file.go` + tests, extended e2e and smoke.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-connect-and-read/01-CONTEXT.md
@.planning/phases/01-connect-and-read/01-RESEARCH.md
@.planning/phases/01-connect-and-read/01-05-SUMMARY.md

<interfaces>
<!-- Existing -->
- tools: Deps, safe, withSubject, toToolText, OutputBudget, Budget, TruncatedFooter, oneLine, resolveRef, refLabel, errEmptyRepo; test helpers newTestSession, callText, newTestDeps
- glclient.NormalizeProject, glclient.NormalizeRepoPath, glclient.ErrBodyTooLarge (mapped to "слишком большой" by toToolText)
- e2e: binPath, envWithout, startSession(t, fakeURL); scripts/smoke.py args --exe, --base-url; prints "SMOKE OK"

<!-- Created here -->
package tools:
- type FileIn struct { Project string `json:"project"`; Path string `json:"path"`; Ref string `json:"ref,omitempty"`; StartLine int `json:"start_line,omitempty"`; EndLine int `json:"end_line,omitempty"` }
- func isBinary(b []byte) bool     // NUL in first 8000 bytes, or !utf8.Valid(prefix) where prefix is first 8000 bytes trimmed back to a rune boundary
- func selectLines(text string, start, end int) (lines []string, from, to, total int, err error)   // 1-based inclusive; 0 = unset; clamp end to total; start > total or end < start -> error

client-go v2:
- c.RepositoryFiles.GetFile(project, path, &gitlab.GetFileOptions{Ref: gitlab.Ptr(ref)}, gitlab.WithContext(ctx)) -> *gitlab.File{FileName, FilePath, Size, Encoding, Content, BlobID, LastCommitID, Ref}
- fake response body for a file: {"file_name":"main.go","file_path":"src/main.go","size":N,"encoding":"base64","content":"<base64>","ref":"main","blob_id":"b1","last_commit_id":"c1"}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_file_contents with base64 decode, line ranges, truncation, binary and oversize guards</name>
  <files>internal/tools/file.go, internal/tools/file_test.go, internal/tools/register.go</files>
  <read_first>
    - internal/tools/tree.go, internal/tools/ref.go, internal/tools/format.go, internal/tools/register.go
    - internal/glclient/transport.go (ErrBodyTooLarge)
    - .planning/phases/01-connect-and-read/01-RESEARCH.md (Pattern 6 get_file_contents algorithm, FileIn struct, wire table file rows)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-09, D-10, D-11)
  </read_first>
  <behavior>
    - {"project":"g/p","path":"src/main.go"} with project default_branch "main": requests "GET /api/v4/projects/g%2Fp" then "GET /api/v4/projects/g%2Fp/repository/files/src%2Fmain%2Ego?ref=main"; text line 1 "src/main.go @ main (ветка по умолчанию) — строки 1-3 из 3, <size> байт", then the 3 lines verbatim
    - with "ref":"feature/x" -> no GetProject request; query ref=feature%2Fx; header without "(ветка по умолчанию)"
    - start_line 2, end_line 3 on a 5-line file -> header "строки 2-3 из 5" and exactly lines 2 and 3
    - start_line 4, end_line 100 on a 5-line file -> lines 4-5, header "строки 4-5 из 5"
    - start_line 9 on a 5-line file -> isError containing "5" (total lines); end_line 1 with start_line 3 -> isError
    - UTF-8 BOM at file start is stripped; CRLF lines are shown without a trailing "\r"; Cyrillic content round-trips unchanged
    - 2000 lines of 20 runes (≈42 000 runes) without range -> body ≤15 000 runes cut on a line boundary, last line "[файл обрезан на 15000 символах: показаны строки 1-K из 2000; используйте start_line/end_line]" with K = number of shown lines
    - content with a NUL byte -> text exactly "src/logo.png @ main (ветка по умолчанию): файл бинарный, <size> байт, blob_id b1", no base64 characters of the content present
    - invalid UTF-8 prefix (0xff 0xfe 0x00-free bytes) -> binary marker; valid UTF-8 whose 8000-byte prefix ends mid-rune -> NOT binary
    - fake route for the file writes a 9 MiB JSON body (content field of repeated 'A') -> isError containing "слишком большой" (default 8 MiB cap from glclient.New)
    - file 404 -> isError containing "404" and "файл"
    - path "" -> isError (schema requires path, or handler error "не указан путь к файлу"); path "../x" -> isError containing ".."
    - empty repo and no ref -> isError containing "репозиторий пуст"
  </behavior>
  <action>
    Create file.go (READ-04) following RESEARCH Pattern 6 exactly: `FileIn` per the interfaces block with jsonschema descriptions (none starting with `word=`): path -> "file path inside the repository, for example src/main.go"; ref -> "branch, tag or commit SHA; default the project's default branch"; start_line -> "first line to return, 1-based; use for big files"; end_line -> "last line to return, inclusive". Plain `int` fields with omitempty (D-15). Handler: NormalizeProject; NormalizeRepoPath and reject empty result with "не указан путь к файлу"; resolveRef (D-11; the files endpoint requires ref); GetFile with errors wrapped `withSubject("файл", err)`; decode `base64.StdEncoding` when Encoding is "base64" (else use Content as-is); `isBinary` (D-10) -> return only the marker line `<path> @ <refLabel>: файл бинарный, <Size> байт, blob_id <BlobID>`; strip BOM; split on "\n" and trim a trailing "\r" per line; `selectLines`; header `<path> @ <refLabel> — строки a-b из N, <Size> байт`; join selected lines, apply `Budget(body, OutputBudget)`; when truncated compute K = number of complete lines kept and append the D-09 footer `[файл обрезан на 15000 символах: показаны строки a-K' из N; используйте start_line/end_line]` where K' = a + K - 1 (the file-specific footer replaces TruncatedFooter for this tool). No pagination footer (single object).

    Register `get_file_contents` in register.go (after list_repository_tree) with safe, ReadOnlyHint annotation, and a ≤900-char description telling the model about default branch, line ranges, the 15 000-character cap and binary handling. Write file_test.go with fake routes and base64-encoded fixtures covering every behavior line; TestToolSchemas must still pass (5 tools).
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/tools/ -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0
    - file.go contains `base64.StdEncoding`, `utf8.Valid`, `файл бинарный`, `используйте start_line/end_line`, `resolveRef(`, `withSubject("файл"`
    - file_test.go asserts RequestURI `/api/v4/projects/g%2Fp/repository/files/src%2Fmain%2Ego?ref=main`
    - `grep -En "\*int|\*string" internal/tools/file.go` outputs nothing
    - register.go contains `"get_file_contents"`
  </acceptance_criteria>
  <done>The agent can read any text file (whole, head-truncated, or by line range) and gets explicit markers for truncation, binary content and oversize files.</done>
</task>

<task type="auto">
  <name>Task 2: Real-binary wire-path e2e and full five-tool Python smoke (hermetic + optional live)</name>
  <files>e2e/stdio_test.go, e2e/python_smoke_test.go, scripts/smoke.py</files>
  <read_first>
    - e2e/stdio_test.go, e2e/main_test.go, e2e/python_smoke_test.go, scripts/smoke.py (current state from Plans 01 and 03)
    - internal/tools/register.go (final tool list)
    - .planning/phases/01-connect-and-read/01-RESEARCH.md (wire table, Test Harness Design tier 3 and 4, Open Questions 1 and 2, Pitfall 12)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-14)
  </read_first>
  <action>
    In `e2e/stdio_test.go` add `TestStdioWirePaths` (D-14, ROADMAP criterion 5): start FakeGitLab with a fallback-friendly route set (register each expected raw path via `f.JSON` returning a valid project/file/tree JSON body), start the real binary via `startSession`, then call over MCP: get_project "group/sub/my.proj" -> expect RequestURI "/api/v4/projects/group%2Fsub%2Fmy%2Eproj"; get_project "gr oup/проект" -> "/api/v4/projects/gr%20oup%2F%D0%BF%D1%80%D0%BE%D0%B5%D0%BA%D1%82"; get_file_contents project "g/p" path "dir with space/a#b.txt" ref "feature/x" -> "/api/v4/projects/g%2Fp/repository/files/dir%20with%20space%2Fa%23b%2Etxt?ref=feature%2Fx"; path "a+b.txt" ref "main" -> ".../files/a+b%2Etxt?ref=main"; path "100%.txt" -> ".../files/100%25%2Etxt?ref=main"; path "файл/имя.go" -> ".../files/%D1%84%D0%B0%D0%B9%D0%BB%2F%D0%B8%D0%BC%D1%8F%2Ego?ref=main"; list_repository_tree project "g/sub/p" path "dir with space/ф+#%" ref "feature/x" page 2 per_page 20 recursive true -> the exact tree RequestURI from the RESEARCH wire table. Also assert each call result is not isError and the list_tools result has exactly the five names whoami, list_projects, get_project, list_repository_tree, get_file_contents.

    Extend `scripts/smoke.py` (QA-02): after the existing checks, assert the tool set equals those five names; add optional args `--project` (default "g/p") and `--file` (default "README.md"); call `list_projects` {"per_page": 5}, `get_project` {"project": project}, `list_repository_tree` {"project": project, "per_page": 1} (prints the pagination footer, RESEARCH Open Q2/A3), `get_file_contents` {"project": project, "path": file, "end_line": 20}, and `get_project` {"project": "no-such-group-xyz/no-such-project"} expecting isError containing "404"; print each text (never the token). Hermetic mode = `--base-url` given. Live mode = no `--base-url` and a real `GITLAB_TOKEN` in env: same calls against gitlab.com, read-only; in live mode require `--project` to be passed explicitly (exit 2 with a message otherwise) and print the first `list_projects` page so the author can eyeball `default_branch` under `simple=true` (A2). Keep final line `SMOKE OK`.

    In `e2e/python_smoke_test.go` register fake routes for all calls the smoke script makes in hermetic mode (`GET /api/v4/user`, `/api/v4/personal_access_tokens/self`, `/api/v4/projects?...` list, `/api/v4/projects/g%2Fp`, tree with `X-Next-Page: 2`, file `README.md` with base64 content, and let the unknown project fall through to the default 404) and pass `--project g/p --file README.md`; assert exit 0, `SMOKE OK`, the tree footer text "есть следующая страница" appears in stdout, and no `glpat-TESTSECRET` in stdout/stderr. Because FakeGitLab matches exact raw paths, route the list endpoint by registering the exact RequestURI path "/api/v4/projects" (query ignored by the router).

    Finally run the release build `CGO_ENABLED=0 go build -trimpath -ldflags "-s -w" -o gitlab-mcp.exe ./cmd/gitlab-mcp` and the full test suite.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go vet ./... && test -z "$(gofmt -l cmd internal e2e)" && go test ./... -count=1 && CGO_ENABLED=0 go build -trimpath -ldflags "-s -w" -o gitlab-mcp.exe ./cmd/gitlab-mcp</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0 including TestStdioWirePaths, TestStdoutPurity, TestPythonSmoke (not skipped: uv is installed on this machine)
    - e2e/stdio_test.go contains `TestStdioWirePaths`, `%D1%84%D0%B0%D0%B9%D0%BB%2F%D0%B8%D0%BC%D1%8F%2Ego`, `a+b%2Etxt`, `100%25%2Etxt`
    - scripts/smoke.py contains `list_projects`, `get_project`, `list_repository_tree`, `get_file_contents`, `whoami`, `--project`, `SMOKE OK`
    - `gitlab-mcp.exe` is produced by the CGO_ENABLED=0 build
  </acceptance_criteria>
  <done>All five tools are proven through the real binary and through the agent's exact client library; wire encoding is locked end to end; the author has a one-command live read-only check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM args -> get_file_contents | project, path, ref, line numbers untrusted |
| repository content -> LLM | file contents are attacker-controllable data |
| smoke script live mode -> gitlab.com | real token used by a dev tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-23 | Denial of service | get_file_contents | mitigate | 8 MiB transport body cap (ErrBodyTooLarge -> readable error), 15 000-rune budget with D-09 footer, binary marker instead of content (D-10), 25 s deadline |
| T-01-24 | Tampering | file path arg | mitigate | NormalizeRepoPath rejects `..`; single encoding verified on the wire through the real binary |
| T-01-25 | Tampering (prompt injection) | returned file content | accept | Content returned as data under a header line; Phase 1 tools are read-only; no server-side action chaining |
| T-01-26 | Information disclosure | scripts/smoke.py | mitigate | Token read from env only, never printed; live mode read-only calls only; tests grep outputs for the test token |
| T-01-27 | Denial of service | line range args | mitigate | start/end clamped to file length; invalid ranges return isError, never panic (safe() recover as backstop) |
</threat_model>

<verification>
- `go vet ./...`, gofmt clean, `go test ./... -count=1` green, release build succeeds
- Manual (optional, not a gate): `GITLAB_TOKEN=<real> uv run scripts/smoke.py --exe gitlab-mcp.exe --project <group/proj>`
</verification>

<success_criteria>
- READ-04 delivered with D-09/D-10/D-11; QA-01 tier 3 wire test and QA-02 smoke cover all five tools
- ROADMAP Phase 1 success criteria 1-5 all backed by automated tests
</success_criteria>

<output>
Create `.planning/phases/01-connect-and-read/01-06-SUMMARY.md` when done
</output>
