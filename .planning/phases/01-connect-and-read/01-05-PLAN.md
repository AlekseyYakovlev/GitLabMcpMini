---
phase: 01-connect-and-read
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - internal/tools/ref.go
  - internal/tools/ref_test.go
  - internal/tools/tree.go
  - internal/tools/tree_test.go
  - internal/tools/register.go
autonomous: true
requirements: [READ-03, FND-04, FND-06]

must_haves:
  truths:
    - "User calls list_repository_tree with a project (ID or path) and optional path/ref and gets one line per entry marking directories and files"
    - "Without ref the tool uses the project's default_branch from GET /projects/:id and names it in the header as '(ветка по умолчанию)'; HEAD is never sent"
    - "An empty repository returns a clear 'репозиторий пуст' text instead of a 404 error"
    - "The tree output shows the pagination footer from X-Next-Page and the truncation footer when over 15 000 characters"
  artifacts:
    - path: "internal/tools/ref.go"
      provides: "resolveRef(ctx, d, project, ref) (ref string, isDefault bool, err error) and errEmptyRepo"
      contains: "func resolveRef"
    - path: "internal/tools/tree.go"
      provides: "list_repository_tree handler and TreeIn"
      contains: "Repositories.ListTree"
  key_links:
    - from: "internal/tools/tree.go"
      to: "resolveRef"
      via: "default branch when ref is empty (D-11)"
      pattern: "resolveRef\\("
    - from: "internal/tools/tree.go"
      to: "glclient.NormalizeRepoPath"
      via: "path argument normalisation"
      pattern: "glclient\\.NormalizeRepoPath"
    - from: "internal/tools/register.go"
      to: "list_repository_tree"
      via: "mcp.AddTool with safe()"
      pattern: "\"list_repository_tree\""
---

## Phase Goal

**As a** автор агента AiAdventAgentV2, **I want to** подключить `gitlab-mcp.exe` по stdio и через него проверять токен и читать мои проекты, дерево файлов и содержимое файлов на gitlab.com, **so that** агент изучает код моих репозиториев стандартным MCP-протоколом без Node/npx и без утечки токена.

<objective>
Vertical slice: the agent can browse a repository's file tree (`list_repository_tree`, READ-03) at any path and ref, defaulting explicitly to the project's default branch (D-11), with pagination and bounded output (FND-06).

Purpose: after this plan a user of the agent can ask "what is in src/ of group/proj on branch feature/x" and get a paginated listing; `resolveRef` is created here and reused by `get_file_contents` in Plan 06.
Output: `ref.go`, `tree.go`, registration, tests.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-connect-and-read/01-CONTEXT.md
@.planning/phases/01-connect-and-read/01-RESEARCH.md
@.planning/phases/01-connect-and-read/01-04-SUMMARY.md

<interfaces>
<!-- Existing -->
- tools: Deps, safe, withSubject, OutputBudget, TruncatedFooter, Budget, ClampPaging, PageFooter, oneLine; test helpers newTestSession, callText
- glclient.NormalizeProject, glclient.NormalizeRepoPath

<!-- Created here -->
package tools:
- var errEmptyRepo = errors.New("репозиторий пуст: в проекте ещё нет коммитов и ветки по умолчанию")
- func resolveRef(ctx context.Context, d Deps, project, ref string) (string, bool, error)
    // ref != "" -> (ref, false, nil) with NO GitLab request; ref == "" -> GetProject; DefaultBranch "" or EmptyRepo -> errEmptyRepo; else (DefaultBranch, true, nil); GetProject errors wrapped withSubject("проект", err)
    // zero state: no cache in Phase 1 (Claude's discretion; RESEARCH Open Q5)
- func refLabel(ref string, isDefault bool) string   // "main (ветка по умолчанию)" or "feature/x"
- type TreeIn struct { Project string `json:"project"`; Path string `json:"path,omitempty"`; Ref string `json:"ref,omitempty"`; Recursive bool `json:"recursive,omitempty"`; Page int `json:"page,omitempty"`; PerPage int `json:"per_page,omitempty"` }

client-go v2:
- c.Repositories.ListTree(project, &gitlab.ListTreeOptions{ListOptions: gitlab.ListOptions{Page: int64(p), PerPage: int64(pp)}, Path: gitlab.Ptr(path), Ref: gitlab.Ptr(ref), Recursive: gitlab.Ptr(true)}, gitlab.WithContext(ctx)) -> ([]*gitlab.TreeNode{ID, Name, Type, Path, Mode}, *gitlab.Response, error); Type is "tree", "blob" or "commit" (submodule)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Default-branch resolution shared by tree and file tools</name>
  <files>internal/tools/ref.go, internal/tools/ref_test.go</files>
  <read_first>
    - internal/tools/projects.go, internal/tools/errors.go, internal/tools/helpers_test.go
    - .planning/phases/01-connect-and-read/01-RESEARCH.md (Pitfall 8 default branch handling, Pattern 6 step 1, Open Question 5)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-11)
  </read_first>
  <behavior>
    - resolveRef(ctx, d, "g/p", "feature/x") -> ("feature/x", false, nil) and fake.Requests() is empty
    - resolveRef(ctx, d, "g/p", "") with fake project default_branch "develop" -> ("develop", true, nil), exactly one request "GET /api/v4/projects/g%2Fp"
    - project with empty_repo true and default_branch "" -> errors.Is(err, errEmptyRepo)
    - fake 404 on the project -> error; toToolText(err) contains "404" and "проект"
    - refLabel("main", true) == "main (ветка по умолчанию)"; refLabel("v1.0", false) == "v1.0"
    - resolveRef never returns "HEAD"
  </behavior>
  <action>
    Implement ref.go per the interfaces block and D-11. Call it with an already-normalised project string. Tests call resolveRef directly with `newTestDeps(t, fake)` (from Plan 03 helpers).
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/tools/ -run 'ResolveRef|RefLabel' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/tools/ -count=1` exits 0
    - ref.go contains `func resolveRef`, `errEmptyRepo`, `ветка по умолчанию`
    - `grep -n '"HEAD"' internal/tools/ref.go` outputs nothing
  </acceptance_criteria>
  <done>One tested function resolves an empty ref to the project's named default branch, or a clear empty-repository error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: list_repository_tree tool end to end</name>
  <files>internal/tools/tree.go, internal/tools/tree_test.go, internal/tools/register.go</files>
  <read_first>
    - internal/tools/register.go, internal/tools/projects.go (pattern for handlers and registration)
    - internal/tools/format.go, internal/tools/ref.go
    - .planning/phases/01-connect-and-read/01-RESEARCH.md (Tool input structs TreeIn, wire table tree row, Pattern 5 recursive note, Pitfall 6 404 label)
    - .planning/phases/01-connect-and-read/01-CONTEXT.md (D-06, D-07, D-11, D-14)
  </read_first>
  <behavior>
    - list_repository_tree {"project":"g/p"} (fake project default_branch "main"; tree returns entries tree "src", blob "README.md", commit "vendor/lib") -> requests: "GET /api/v4/projects/g%2Fp" then a tree request whose query contains ref=main, page=1, per_page=20; text first line "g/p @ main (ветка по умолчанию), путь: /"; entry lines "dir  src/", "file README.md", "sub  vendor/lib"; last line last-page footer
    - {"project":"g/sub/p","path":"dir with space/ф+#%","ref":"feature/x","recursive":true,"page":2,"per_page":20} -> only ONE request (no GetProject) with RequestURI exactly "/api/v4/projects/g%2Fsub%2Fp/repository/tree?page=2&path=dir+with+space%2F%D1%84%2B%23%25&per_page=20&recursive=true&ref=feature%2Fx"; header shows "g/sub/p @ feature/x, путь: dir with space/ф+#%"
    - fake tree response with header X-Next-Page: 3 on page 2 -> footer "[page 2, per_page 20 — есть следующая страница: вызовите с page=3]"
    - path "a/../b" -> isError containing "..", no requests
    - empty repo (no ref given, project empty_repo true) -> isError true with text containing "репозиторий пуст"
    - tree 404 with ref given -> isError text contains "404" and "путь или ref"
    - empty tree array -> text contains "пусто" after the header
    - 100 entries with 200-rune paths -> TruncatedFooter present, result < 15500 runes
  </behavior>
  <action>
    Create tree.go (READ-03): `TreeIn` exactly as in the interfaces block with jsonschema descriptions (none starting with `word=`): project -> same text as ProjectIn; path -> "directory inside the repository, default repository root"; ref -> "branch, tag or commit SHA; default the project's default branch"; recursive -> "list all nested entries; output is capped at 15000 characters"; page/per_page as in list_projects. Handler: NormalizeProject, NormalizeRepoPath, ClampPaging, `resolveRef` (D-11); `ListTree` with Path set only when non-empty, Ref always set to the resolved ref (never HEAD), Recursive only when true; errors wrapped `withSubject("путь или ref в дереве", err)`. Header line `<project> @ <refLabel>, путь: <path or "/">`; entry lines: type tree -> `dir  <path>/`, blob -> `file <path>`, commit -> `sub  <path>`, other -> `<type> <path>`; use node.Path (full path) so recursive output is unambiguous. Empty listing -> "(пусто)". Compose with Budget + TruncatedFooter + PageFooter (the defensive NextLink check covers RESEARCH A3). Register with `mcp.AddTool` + safe + ReadOnlyHint annotation and a ≤900-char description explaining path/ref/recursive/pagination. Keep the order of registered tools: whoami, list_projects, get_project, list_repository_tree.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0 (TestToolSchemas now covers 4 tools)
    - tree_test.go contains the exact expected RequestURI `/api/v4/projects/g%2Fsub%2Fp/repository/tree?page=2&path=dir+with+space%2F%D1%84%2B%23%25&per_page=20&recursive=true&ref=feature%2Fx`
    - tree.go contains `resolveRef(`, `glclient.NormalizeRepoPath`, `Repositories.ListTree`, `PageFooter(`
    - register.go contains `"list_repository_tree"`
  </acceptance_criteria>
  <done>The agent can list a repository tree at any path/ref with pagination; missing ref resolves to the named default branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM args -> list_repository_tree | project, path, ref untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | Tampering | tree path arg | mitigate | NormalizeRepoPath rejects `..`, normalises `\`; client-go encodes the query once (wire test) |
| T-01-21 | Denial of service | recursive tree | mitigate | per_page ≤100, 15 000-rune Budget with explicit footer, 25 s call deadline |
| T-01-22 | Information disclosure | error wording | mitigate | withSubject + toToolText; 404 text notes private-project visibility without echoing bodies; safe() redacts |
</threat_model>

<verification>
- `go test ./... -count=1` green; `go vet ./...` clean
</verification>

<success_criteria>
- READ-03 delivered; D-11 enforced via resolveRef; wire path for tree locked by test
</success_criteria>

<output>
Create `.planning/phases/01-connect-and-read/01-05-SUMMARY.md` when done
</output>
