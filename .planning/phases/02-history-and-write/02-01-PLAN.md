---
phase: 02-history-and-write
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/testutil/fakegitlab.go
  - internal/tools/branch.go
  - internal/tools/branch_test.go
  - internal/tools/errors.go
  - internal/tools/errors_test.go
  - internal/tools/register.go
  - e2e/stdio_test.go
  - e2e/python_smoke_test.go
  - scripts/smoke.py
autonomous: true
requirements: [READ-05, WRT-01]

must_haves:
  truths:
    - "User calls list_branches and gets one line per branch: name, short SHA, markers [default]/[protected]/[merged], last commit title; search and page/per_page work; the footer says whether there is a next page (also when GitLab only sends a Link header) (D-15)"
    - "User calls create_branch with branch (and optional ref; default = project default branch) and gets name, source ref, tip SHA and web_url; the new branch then appears in list_branches"
    - "create_branch for an existing branch returns isError '<status>: ветка уже существует ...' where <status> is the HTTP status GitLab actually returned (400, or 409/422 if GitLab answers so), and nothing else changes"
    - "A write that fails with 5xx/timeout/network is sent exactly once and the text says 'Результат записи неизвестен'"
    - "403/401 on a write explain protected branch / scope api / role Developer+ in Russian; a 404 on create_branch reads 'не найдено (проект или ветка)'"
    - "The real binary lists 7 tools and the Python mcp 1.30.0 smoke calls list_branches and create_branch successfully (D-17)"
  artifacts:
    - path: "internal/tools/branch.go"
      provides: "list_branches and create_branch handlers, BranchesIn, CreateBranchIn, branchLine"
      contains: "Branches.CreateBranch"
    - path: "internal/tools/errors.go"
      provides: "withWrite(op, subject, err), op constants, statusPrefix and write-aware wording in toToolText"
      contains: "func withWrite"
    - path: "internal/testutil/fakegitlab.go"
      provides: "request body capture via Recorded()"
      contains: "func (f *FakeGitLab) Recorded"
  key_links:
    - from: "internal/tools/branch.go"
      to: "resolveRef"
      via: "empty ref -> project default branch (D-16)"
      pattern: "resolveRef\\("
    - from: "internal/tools/branch.go"
      to: "withWrite"
      via: "CreateBranch error wrapping, keyed on the write op, subject 'проект или ветка'"
      pattern: "withWrite\\(opCreateBranch, \"проект или ветка\""
    - from: "internal/tools/register.go"
      to: "createBranch"
      via: "mcp.AddTool with DestructiveHint false"
      pattern: "\"create_branch\""
---

## Phase Goal

**As a** автор агента AiAdventAgentV2, **I want to** через `gitlab-mcp.exe` просматривать ветки, историю коммитов и различия и вносить изменения в репозиторий (создать ветку, закоммитить один или несколько файлов), **so that** агент может не только читать код, но и готовить изменения для будущего Merge Request.

<objective>
First vertical slice of Phase 2: the agent sees the branches of a project (READ-05, D-15) and creates its own branch (WRT-01, D-16), then sees it in the list (ROADMAP SC-2). This slice also introduces the write-side error wording (`withWrite`, D-09 base rules) and request-body capture in the fake GitLab, both reused by later write slices.

Purpose: an agent must pick or create a safe branch before any commit; `[protected]`/`[default]` markers steer it away from writing into main.
Output: `branch.go` + tests, write-aware `errors.go`, `FakeGitLab.Recorded()`, 7-tool e2e/smoke.

Accepted risk (RESEARCH Open Question 1, resolved): GitLab may paginate `GET /repository/branches` by keyset, answering with only a `Link rel="next"` header and ignoring `page`. The plan handles the Link-only answer through the Phase 1 `PageFooter` NextLink fallback and a fake test, but whether `page=N` is honoured on gitlab.com cannot be proven offline. The risk is accepted; the Phase 4 live run verifies it. The SUMMARY must carry this item forward (see `<output>`).
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-history-and-write/02-CONTEXT.md
@.planning/phases/02-history-and-write/02-RESEARCH.md
@.planning/phases/02-history-and-write/02-PATTERNS.md

<interfaces>
<!-- Existing (Phase 1), use directly -->
package tools:
- type Deps struct { GL *gitlab.Client; Logger *slog.Logger; Redact func(string) string; Timeout time.Duration }
- func safe[T any](d Deps, h func(ctx context.Context, in T) (string, error)) mcp.ToolHandlerFor[T, any]
- func resolveRef(ctx context.Context, d Deps, project, ref string) (ref string, isDefault bool, err error); func refLabel(ref string, isDefault bool) string
- const OutputBudget = 15000; func Budget(body string, limit int) (string, bool); func ClampPaging(page, perPage int) (int, int); func PageFooter(page, perPage int, resp *gitlab.Response) string; func composeList(body string, page, perPage int, resp *gitlab.Response) string; func oneLine(s string, max int) string; TruncatedFooter
- type subjectError struct { subject string; err error }; func withSubject(subject string, err error) error; func toToolText(err error) string; func capDetail(s string) string
- tests: newTestSession(t, fake), callText(t, cs, name, args) (text, isError) — fails the test if the token leaks; newTestDeps; requestsTo(fake, prefix) []string (projects_test.go); treeProjectJSON const (tree_test.go)
package glclient:
- func NormalizeProject(s string) (string, error); func NormalizeRepoPath(p string) (string, error)
- func Classify(err error) *Error; type Error struct { Kind Kind; Status int; Detail string; RetryAfter time.Duration }
- Kinds: KindOther, KindUnauthorized, KindForbidden, KindNotFound, KindRateLimited, KindServer, KindBadRequest, KindNetwork, KindTimeout, KindCanceled, KindTooLarge, KindDecode
- NOTE: classifyResponse maps every status >= 400 without a more specific rule (e.g. 400, 409, 422) to KindBadRequest; e.Status holds the real code
package testutil:
- func NewFakeGitLab(t testing.TB) *FakeGitLab; (f) JSON(method, rawPath string, status int, body string, headers map[string]string); (f) Handle(method, rawPath string, h http.HandlerFunc); (f) Requests() []string ("METHOD RequestURI"); (f) Reset(); f.URL
package e2e: testToken = "glpat-TESTSECRET"; newFakeWithWhoami(t) *testutil.FakeGitLab; startSession(t, fakeURL); binPath

client-go v2.64.0:
- d.GL.Branches.ListBranches(pid, &gitlab.ListBranchesOptions{ListOptions: gitlab.ListOptions{Page, PerPage int64}, Search *string}, gitlab.WithContext(ctx)) -> ([]*gitlab.Branch, *gitlab.Response, error)
- d.GL.Branches.CreateBranch(pid, &gitlab.CreateBranchOptions{Branch *string, Ref *string}, ...) -> (*gitlab.Branch, *gitlab.Response, error)   // POST /projects/:id/repository/branches, JSON body
- type Branch struct { Commit *Commit; Name string; Protected, Merged, Default, CanPush bool; WebURL string }
- type Commit struct { ID, ShortID, Title, AuthorName string; CommittedDate *time.Time; Message string; ParentIDs []string; Stats *CommitStats; WebURL string; ... }

<!-- Created here -->
package testutil:
- type Recorded struct { Method, URI, Body string }; func (f *FakeGitLab) Recorded() []Recorded
package tools:
- type BranchesIn struct { Project string `json:"project"`; Search string `json:"search,omitempty"`; Page int `json:"page,omitempty"`; PerPage int `json:"per_page,omitempty"` }
- type CreateBranchIn struct { Project string `json:"project"`; Branch string `json:"branch"`; Ref string `json:"ref,omitempty"` }
- const opCreateBranch = "create_branch"; const opCommit = "commit"   // write operation keys; opCommit is used by commitCore in Plan 04 (commit_files and create_or_update_file)
- func withWrite(op, subject string, err error) error   // subjectError with write=true and op; nil stays nil
- func statusPrefix(e *glclient.Error) string   // fmt.Sprintf("%d: ", e.Status); "400: " when e.Status == 0
- type writeRule struct { kind glclient.Kind; op string /* "" = any */; substrings []string /* lowercase, any-of */; text string /* WITHOUT status prefix */ }
- func writeText(e *glclient.Error, op, subject string) (string, bool)   // write-only wording, consulted by toToolText before the generic switch
- func baseText(e *glclient.Error, subject string) string   // the former generic switch of toToolText
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: list_branches and create_branch end to end (in-memory MCP session over fake GitLab)</name>
  <files>internal/testutil/fakegitlab.go, internal/tools/branch.go, internal/tools/branch_test.go, internal/tools/errors.go, internal/tools/register.go</files>
  <read_first>
    - internal/testutil/fakegitlab.go (serve, Requests, Reset)
    - internal/tools/tree.go, internal/tools/projects.go (handler and line-renderer patterns), internal/tools/ref.go, internal/tools/format.go
    - internal/tools/errors.go (subjectError, withSubject, toToolText)
    - internal/tools/register.go, internal/tools/tree_test.go, internal/tools/helpers_test.go
    - .planning/phases/02-history-and-write/02-CONTEXT.md (D-15, D-16, D-17)
    - .planning/phases/02-history-and-write/02-PATTERNS.md (sections branch.go, fakegitlab.go, register.go)
  </read_first>
  <behavior>
    - list_branches {"project":"g/p"} -> one request "GET /api/v4/projects/g%2Fp/repository/branches?page=1&per_page=20"; header line "ветки g/p"; branch main (default, protected, commit short_id a1b2c3d4, title "Init") renders exactly "main a1b2c3d4 [default] [protected] Init"; branch feature/x (merged) renders "feature/x 9f8e7d6c [merged] <title>"
    - list_branches with "search":"feat" -> query contains search=feat; without search the key is absent
    - branch with "commit": null renders the name and "-" instead of the SHA, no panic
    - X-Next-Page: 2 -> text ends with the Phase 1 next-page footer; response with only a Link rel="next" header (no X-Next-Page, the keyset-style answer) -> footer still says there is a next page
    - empty list -> "ветки не найдены"
    - create_branch {"project":"g/p","branch":"feature/x","ref":"main"} -> no GET project; exactly one "POST /api/v4/projects/g%2Fp/repository/branches"; Recorded body JSON decodes to {"branch":"feature/x","ref":"main"}; text contains "feature/x", "main", the full commit ID and the web_url
    - create_branch without ref -> "GET /api/v4/projects/g%2Fp" first, body ref = project default_branch, text contains "(ветка по умолчанию)"
    - create_branch with branch "  " -> isError "не указано имя ветки", zero requests
    - create_branch against POST 404 {"message":"404 Project Not Found"} -> isError containing "проект или ветка" (never the bare "(ветка)")
    - SC-2: with a stateful fake (POST handler appends the new branch, GET handler returns the current slice) create_branch "feature/new" then list_branches -> list text contains "feature/new"
    - Recorded() returns Method, full RequestURI and body for every request; Requests() output is unchanged; Reset() clears both
  </behavior>
  <action>
    FakeGitLab: in serve(), read r.Body with io.ReadAll (nil body -> empty string), restore it with io.NopCloser(bytes.NewReader(b)) before calling the handler, append Recorded{Method, URI: r.RequestURI, Body} to a new slice under the mutex; add Recorded() returning a copy; Reset() clears it too. Requests() keeps its exact current output.

    errors.go: add `write bool` and `op string` fields to subjectError, constants opCreateBranch = "create_branch" and opCommit = "commit", and `func withWrite(op, subject string, err error) error` (nil stays nil) returning &subjectError{subject: subject, err: err, write: true, op: op}. The op key (not the subject text) is what later rules match on, so write subjects can stay user-meaningful (per checker WARNING 5). In this task toToolText behaves as before for write errors (the rules come in Task 2).

    branch.go (READ-05 per D-15, WRT-01 per D-16): constants listBranchesDescription and createBranchDescription in Russian, each ≤900 runes. list_branches description must say the markers mean: [default] ветка по умолчанию, [protected] ветка может быть защищена (писать в свою ветку, созданную create_branch), [merged] уже влита. create_branch description: создаёт ветку от ref (SHA, ветка или тег; по умолчанию ветка проекта), существующая ветка даёт ошибку, запись требует токен со scope api. Inputs BranchesIn and CreateBranchIn exactly as in the interfaces block, plain fields, jsonschema tags in English not starting with `word=` (search -> "substring of the branch name to filter by"; branch -> "name of the new branch, for example feature/x"; ref -> "branch, tag or commit SHA to branch from; default the project's default branch"; project/page/per_page copied from TreeIn). listBranches(d): NormalizeProject -> ClampPaging -> ListBranchesOptions (Search only when non-empty after TrimSpace) -> error `withSubject("проект", err)` -> header "ветки <project>" (append ", поиск: <search>" when set) -> branchLine per item -> composeList (its PageFooter already falls back to the Link rel="next" header, which covers a keyset-style answer; no page_token handling is added — accepted risk, verified live in Phase 4). branchLine(b *gitlab.Branch): D-15 order "name shortSHA [default] [protected] [merged] title" with title via oneLine(title, 100) (define const branchTitleRunes = 100); nil Commit -> SHA "-" and no title. createBranch(d): NormalizeProject; branch := TrimSpace(in.Branch), empty -> errors.New("не указано имя ветки"); resolveRef(ctx, d, project, TrimSpace(in.Ref)); CreateBranch with gitlab.Ptr(branch)/gitlab.Ptr(ref); error -> `withWrite(opCreateBranch, "проект или ветка", err)` (same subject as commitCore, so a 404 reads "не найдено (проект или ветка)"); output lines "ветка <name> создана от <refLabel(ref,isDefault)>", "конец ветки: <Commit.ID>" (or "-" when Commit is nil), "<WebURL>". No retry loop and no pre-check GET of the branch (D-05/FND-07; the client-level GET-only retry policy already applies).

    register.go: add list_branches (Annotations ReadOnlyHint true) and create_branch (Annotations: DestructiveHint pointing to a false bool — additive write, per Claude's discretion) with safe(d, ...). Write branch_test.go covering every behavior line; for the Link-only case set header `Link: <FAKEURL/api/v4/projects/g%2Fp/repository/branches?page=2&per_page=20>; rel="next"`.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/tools/ ./internal/testutil/ -count=1 -run "Branch|Tool|Schema|ToToolText"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/tools/ -count=1` exits 0 (TestToolSchemas passes with the two new tools)
    - branch.go contains `Branches.ListBranches`, `Branches.CreateBranch`, `resolveRef(`, `withWrite(opCreateBranch, "проект или ветка"`, `[protected]`, `[default]`, `[merged]`
    - `grep -v '^\s*//' internal/tools/branch.go | grep -c 'withWrite("ветка"'` prints 0
    - errors.go contains `func withWrite(op, subject string, err error) error`, `opCreateBranch`, `opCommit`
    - fakegitlab.go contains `io.ReadAll`, `io.NopCloser`, `func (f *FakeGitLab) Recorded() []Recorded`
    - `grep -En "\*int|\*string|\*bool" internal/tools/branch.go` outputs nothing
    - branch_test.go contains `feature/new` (SC-2 test), a Link-only footer case, and decodes the POST body with `json.Unmarshal`
  </acceptance_criteria>
  <done>The agent can list branches with protection markers and create its own branch; the created branch is visible in the list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write-aware error wording (branch exists, bad ref/name, 401/403 on write, outcome unknown on 5xx/timeout)</name>
  <files>internal/tools/errors.go, internal/tools/errors_test.go, internal/tools/branch_test.go</files>
  <read_first>
    - internal/tools/errors.go, internal/tools/errors_test.go (TestToToolText table fields prefix/contains/notContains/exact)
    - internal/glclient/errors.go (Classify, classifyResponse status mapping, Kind constants, Detail format)
    - .planning/phases/02-history-and-write/02-RESEARCH.md (Pattern 3 table, Pitfall 3, Pitfall 4)
    - .planning/phases/02-history-and-write/02-CONTEXT.md (D-05, D-09, D-16)
  </read_first>
  <behavior>
    - withWrite(opCreateBranch, "проект или ветка", Status 400 Detail "{message: Branch already exists}") -> starts with "400: ветка уже существует"
    - same error with Status 409 -> starts with "409: ветка уже существует" and does not contain "400:"
    - withWrite(opCreateBranch, ..., 400 Detail "{message: Failed to create branch 'x': invalid reference name 'nope'}") -> starts with "400: исходный ref не найден" and contains the capped detail; the same detail with Status 422 -> starts with "422: исходный ref не найден"
    - withWrite(opCreateBranch, ..., 400 Detail "{message: Branch name is invalid}") -> starts with "400: недопустимое имя ветки"
    - withWrite(opCommit, "проект или ветка", 400 Detail "{message: Branch already exists}") -> does NOT contain "ветка уже существует" (branch-exists rule is keyed on op, not subject) and equals the generic 400 text
    - withWrite(any op, 403) -> starts with "403: запись отклонена" and contains "ветка может быть защищена", "scope `api`", "Developer", "create_branch"
    - withWrite(any op, 401) -> contains "401" and "scope `api`"
    - withWrite(any op, KindServer 503), KindTimeout, KindNetwork -> base text of the kind plus "Результат записи неизвестен"
    - withWrite(any op, 400 unmatched detail) -> identical to the existing generic 400 text
    - withSubject (read) errors -> all existing TestToToolText cases unchanged
    - tool level: create_branch against POST 503 -> exactly 1 POST in fake.Requests() and text contains "Результат записи неизвестен"; POST 400 {"message":"Branch already exists"} -> "400: ветка уже существует"; POST 409 with the same body -> "409: ветка уже существует"
  </behavior>
  <action>
    In errors.go add `func statusPrefix(e *glclient.Error) string` returning fmt.Sprintf("%d: ", e.Status), or "400: " when e.Status == 0, and `func writeText(e *glclient.Error, op, subject string) (string, bool)`; call writeText in toToolText right after Classify and before the generic switch, only when the unwrapped subjectError has write == true. Implement the rules as an ordered package-level table `var writeRules = []writeRule{...}` (struct fields kind, op, substrings, text exactly as in the interfaces block) so later plans append commit/file rules. A rule matches when e.Kind == kind, rule.op is empty or equals the error's op, and any lowercase substring is contained in strings.ToLower(e.Detail) (never equality — Detail looks like "{message: ...}"). Rule texts carry NO status digits; writeText returns statusPrefix(e) + text (+ " " + capDetail(e.Detail) where stated), because classifyResponse maps 409/422 and other unmapped statuses to KindBadRequest and the message must show the real status (checker WARNING 4). Rules in this plan (D-09, D-16, RESEARCH Pattern 3): (1) KindBadRequest + op opCreateBranch + "already exists" -> "ветка уже существует: выберите другое имя или используйте существующую ветку." (keyed on the op, per WARNING 5); (2) KindBadRequest + op opCreateBranch + "branch name is invalid" -> "недопустимое имя ветки: " + capDetail; (3) KindBadRequest + any op + any of "invalid reference name", "ref is missing" -> "исходный ref не найден: укажите существующую ветку, тег или SHA. " + capDetail. Kind-level write handling after the table: KindForbidden -> statusPrefix(e) + "запись отклонена: ветка может быть защищена, у токена может не быть scope `api`, или ваша роль в проекте ниже Developer. Создайте свою ветку (create_branch), коммитьте в неё, затем откройте MR."; KindUnauthorized -> existing 401 text + " Для записи нужен токен со scope `api`."; KindServer, KindTimeout, KindNetwork -> existing text of that kind + " Результат записи неизвестен: изменение могло быть применено. Проверьте состояние (list_branches, list_commits) перед повтором." (Pitfall 4, FND-07). Everything else returns ok=false and falls through to the generic switch. To reuse the base texts without duplication, extract the generic switch into `func baseText(e *glclient.Error, subject string) string` and have toToolText call it. Leave the Phase 1 read-side wording in baseText untouched.

    Extend errors_test.go TestToToolText with the write cases above (build errors as &glclient.Error{Kind, Status, Detail} wrapped by withWrite(op, subject, ...)) and add the three tool-level tests to branch_test.go (POST 503 exactly once; 400 already exists; 409 already exists).
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go test ./internal/tools/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/tools/ -count=1` exits 0, including all pre-existing TestToToolText cases
    - errors.go contains `func writeText(`, `func baseText(`, `func statusPrefix(`, `var writeRules`, `Результат записи неизвестен`, `ветка уже существует`, and the substring scope `api` (with backticks around api)
    - `grep -v '^\s*//' internal/tools/errors.go | grep -cE 'text: *"[0-9]{3}: '` prints 0 (no hard-coded status in rule texts)
    - errors_test.go contains a case with Status 409 expecting prefix "409: ветка уже существует" and a Status 422 case expecting prefix "422: исходный ref не найден"
    - branch_test.go asserts `len(requestsTo(fake, "POST /api/v4/projects/g%2Fp/repository/branches")) == 1` for the 503 case
  </acceptance_criteria>
  <done>Write failures produce actionable Russian messages that show the real HTTP status; ambiguous failures warn that the write may have happened; writes are sent once.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the branch slice through the real binary and the Python mcp 1.30.0 client</name>
  <files>e2e/stdio_test.go, e2e/python_smoke_test.go, scripts/smoke.py</files>
  <read_first>
    - e2e/stdio_test.go (wantNames list near the ListTools assertion), e2e/main_test.go, e2e/python_smoke_test.go
    - scripts/smoke.py (EXPECTED_TOOLS, run(), call helper, live vs hermetic modes)
    - internal/tools/register.go
  </read_first>
  <action>
    e2e/stdio_test.go: extend the sorted wantNames to the 7 tools: create_branch, get_file_contents, get_project, list_branches, list_projects, list_repository_tree, whoami.

    scripts/smoke.py: add "list_branches" and "create_branch" to EXPECTED_TOOLS. After the existing calls add `list_branches` {"project": project, "per_page": 5} in both modes. Only when base_url is set (hermetic mode; live mode stays read-only as documented in the module docstring) call `create_branch` {"project": project, "branch": "smoke/branch", "ref": "main"} and check the text contains "smoke/branch". Update the docstring line about live mode to say write tools are called only in hermetic mode.

    e2e/python_smoke_test.go: register `GET /api/v4/projects/g%2Fp/repository/branches` returning one branch main (default, protected, commit short_id "a1b2c3d4", title "Init") and `POST /api/v4/projects/g%2Fp/repository/branches` returning 201 with {"name":"smoke/branch","commit":{"id":"a1b2c3d4e5f6a7b8c9d0a1b2c3d4e5f6a7b8c9d0","short_id":"a1b2c3d4","title":"Init"},"web_url":"https://gitlab.example/g/p/-/tree/smoke/branch"}; assert stdout contains "[protected]" and "smoke/branch". Keep all existing assertions.
  </action>
  <verify>
    <automated>cd /c/Projects/GitLabMcpMini && go vet ./... && test -z "$(gofmt -l cmd internal e2e)" && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0 with TestPythonSmoke not skipped (uv is installed)
    - scripts/smoke.py contains `"list_branches"`, `"create_branch"`, `smoke/branch` and the create_branch call is inside an `if base_url` branch
    - e2e/stdio_test.go wantNames contains `"create_branch"` and `"list_branches"`
    - e2e/python_smoke_test.go contains `POST", "/api/v4/projects/g%2Fp/repository/branches`
  </acceptance_criteria>
  <done>The agent's own client library sees 7 tools and can list and create branches through the real binary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM args -> list_branches/create_branch | project, search, branch, ref are untrusted strings |
| server -> gitlab.com (POST) | first state-changing request; authorised only by the PAT and GitLab roles |
| GitLab error bodies -> LLM | error detail is external text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | create_branch args | mitigate | NormalizeProject on project; branch/ref sent only as JSON body values via client-go option structs (no manual URL building); empty branch rejected before any request |
| T-02-02 | Elevation of privilege | create_branch | accept | Server enforces nothing beyond GitLab: authorisation is the PAT scope and project role (PROJECT.md "запись без ограничений", read-only mode out of scope); 403 wording explains it |
| T-02-03 | Repudiation / Tampering | write retries | mitigate | No retry of POST (client GET/HEAD-only policy); tool test asserts exactly one POST on 503; "Результат записи неизвестен" wording prevents blind duplicate retries |
| T-02-04 | Information disclosure | error texts | mitigate | GitLab detail capped via capDetail (300 runes); safe() redacts the token last; callText test helper fails on token leaks |
| T-02-05 | Denial of service | list_branches | mitigate | per_page clamped to 100, OutputBudget 15 000 runes with TruncatedFooter, 25 s call deadline |
</threat_model>

<verification>
- `go vet ./...`, gofmt clean, `go test ./... -count=1` green
</verification>

<success_criteria>
- READ-05 and WRT-01 delivered per D-15/D-16; ROADMAP Phase 2 SC-2 covered by a test (create then list)
- Write error base rules (D-09 403/401, outcome-unknown) in place for later write slices, with the status prefix taken from the actual response and rules keyed on the write op
</success_criteria>

<output>
Create `.planning/phases/02-history-and-write/02-01-SUMMARY.md` when done. The SUMMARY must include a "Carried to Phase 4" item: "list_branches pagination on gitlab.com: GitLab may answer with keyset `Link rel=next` and ignore `page`; Link-only answer is covered by the PageFooter fallback and a fake test; the live run must confirm that `page=2` returns the second page (or record keyset behaviour and adjust the hint)."
</output>
