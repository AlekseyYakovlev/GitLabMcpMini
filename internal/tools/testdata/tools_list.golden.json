[
  {
    "annotations": {
      "destructiveHint": true,
      "idempotentHint": false,
      "readOnlyHint": false
    },
    "description": "Один коммит с несколькими файлами в существующую ветку (создайте её через create_branch; в защищённую ветку не писать). Действия: create, update, delete, move (для move обязателен previous_path — старый путь). Только UTF-8 текст, до 50 файлов и до 1 МБ суммарно; content — полный новый текст файла. commit_files не проверяет конкурентные правки; для одного файла с проверкой используйте create_or_update_file. Запрос не повторяется автоматически. В ответе: SHA коммита (его можно передать в get_commit), +/- по файлам и ссылка. Нужен токен со scope api.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "actions": {
          "description": "file changes of one commit, 1..50 items",
          "items": {
            "additionalProperties": false,
            "properties": {
              "action": {
                "description": "create, update, delete or move",
                "type": "string"
              },
              "content": {
                "description": "full new UTF-8 text content; required for create and update, optional for move",
                "type": "string"
              },
              "file_path": {
                "description": "path of the file in the repository",
                "type": "string"
              },
              "previous_path": {
                "description": "old path, only for move",
                "type": "string"
              }
            },
            "required": [
              "action",
              "file_path"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "branch": {
          "description": "existing branch to commit to, for example feature/x (create it with create_branch)",
          "type": "string"
        },
        "commit_message": {
          "description": "commit message",
          "type": "string"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        }
      },
      "required": [
        "project",
        "branch",
        "commit_message",
        "actions"
      ],
      "type": "object"
    },
    "name": "commit_files"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Сравнение двух веток, тегов или коммитов GitLab-проекта: from — база, to — то, что с ней сравнивается; как в GitLab, сравнение идёт от общего предка (merge-base). Вывод: коммиты (до 20 строк) и diff по файлам, то есть что принесёт ветка to относительно from — удобно проверить перед Merge Request. page и per_page листают файлы; patch каждого файла ограничен 2000 символами, весь вывод — 15000. Если GitLab прервал сравнение по таймауту, об этом сказано явно.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "from": {
          "description": "base branch, tag or commit SHA",
          "type": "string"
        },
        "page": {
          "description": "page of the file list, default 1",
          "type": "integer"
        },
        "per_page": {
          "description": "files per page, default 20, max 100",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "to": {
          "description": "branch, tag or commit SHA to compare with the base",
          "type": "string"
        }
      },
      "required": [
        "project",
        "from",
        "to"
      ],
      "type": "object"
    },
    "name": "compare_refs"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": false,
      "readOnlyHint": false
    },
    "description": "Создаёт новую ветку в GitLab-проекте от ref (коммит SHA, ветка или тег; по умолчанию — ветка по умолчанию проекта). Если ветка с таким именем уже есть, вернётся ошибка и ничего не изменится. В ответе: имя ветки, от чего создана, SHA конца ветки и ссылка. Запись требует токен со scope api и роль Developer или выше. Дальше коммитьте в созданную ветку и открывайте Merge Request.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "branch": {
          "description": "name of the new branch, for example feature/x",
          "type": "string"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "ref": {
          "description": "branch, tag or commit SHA to branch from; default the project's default branch",
          "type": "string"
        }
      },
      "required": [
        "project",
        "branch"
      ],
      "type": "object"
    },
    "name": "create_branch"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": false,
      "readOnlyHint": false
    },
    "description": "Создаёт Merge Request из source_branch в target_branch (по умолчанию — ветка по умолчанию проекта). draft=true помечает MR как Draft через префикс «Draft:» в заголовке. Вернётся ошибка, если из этой ветки уже есть открытый MR или между ветками нет различий (сначала закоммитьте изменения через commit_files). Строки описания, начинающиеся с «/», GitLab выполняет как quick actions. Сразу после создания статус слияния обычно checking: перед merge_merge_request вызовите get_merge_request. Запрос никогда не повторяется автоматически. Запись требует токен со scope api и роль Developer или выше.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "description": {
          "description": "merge request description in Markdown",
          "type": "string"
        },
        "draft": {
          "description": "true marks the MR as Draft (title prefix Draft:)",
          "type": "boolean"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "source_branch": {
          "description": "branch with your changes",
          "type": "string"
        },
        "target_branch": {
          "description": "branch to merge into; default the project's default branch",
          "type": "string"
        },
        "title": {
          "description": "merge request title",
          "type": "string"
        }
      },
      "required": [
        "project",
        "source_branch",
        "title"
      ],
      "type": "object"
    },
    "name": "create_merge_request"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": false,
      "readOnlyHint": false
    },
    "description": "Добавляет общий (не построчный) комментарий к Merge Request: iid — номер после ! в GitLab, body — текст в Markdown, не должен быть пустым. Строки, начинающиеся с «/», GitLab выполняет как quick actions (например /close). Перед записью MR читается один раз, чтобы вернуть ссылку на него и сразу сообщить, если MR нет. Запрос никогда не повторяется автоматически. Запись требует токен со scope api. Заметки MR показывает list_merge_request_notes.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "body": {
          "description": "comment text in Markdown",
          "type": "string"
        },
        "iid": {
          "description": "merge request IID, the number after ! in GitLab",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        }
      },
      "required": [
        "project",
        "iid",
        "body"
      ],
      "type": "object"
    },
    "name": "create_merge_request_note"
  },
  {
    "annotations": {
      "destructiveHint": true,
      "idempotentHint": false,
      "readOnlyHint": false
    },
    "description": "Создаёт или обновляет один текстовый файл одним коммитом в существующую ветку (создайте её через create_branch). Сам определяет, создать файл или обновить, и пишет об этом в ответе (created/updated). Защищает от перезаписи чужих правок: при обновлении передаётся last_commit_id, прочитанный с этой же ветки; при конфликте перечитайте файл и повторите. Содержимое пишется как есть; если в файле были CRLF, а в новом тексте их нет, будет предупреждение. Для нескольких файлов используйте commit_files. Запрос не повторяется автоматически. Нужен токен со scope api.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "branch": {
          "description": "existing branch to commit to; create it first with create_branch",
          "type": "string"
        },
        "commit_message": {
          "description": "commit message, not empty",
          "type": "string"
        },
        "content": {
          "description": "full new UTF-8 text content of the file",
          "type": "string"
        },
        "path": {
          "description": "file path inside the repository",
          "type": "string"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        }
      },
      "required": [
        "project",
        "path",
        "content",
        "branch",
        "commit_message"
      ],
      "type": "object"
    },
    "name": "create_or_update_file"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Один коммит GitLab-проекта по SHA, ветке или тегу: шапка (полный SHA, автор, дата, родители, число добавленных и удалённых строк, сообщение, ссылка) и diff по файлам. Patch каждого файла ограничен 2000 символами, весь вывод — 15000. Если patch у файла не показан, рядом указана причина (too_large, collapsed, только переименование, только смена режима, пустой или бинарный файл) и как прочитать содержимое через get_file_contents. page и per_page листают файлы diff.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "page": {
          "description": "page of the file list of the diff, default 1",
          "type": "integer"
        },
        "per_page": {
          "description": "files per page, default 20, max 100",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "sha": {
          "description": "commit SHA, branch or tag name",
          "type": "string"
        }
      },
      "required": [
        "project",
        "sha"
      ],
      "type": "object"
    },
    "name": "get_commit"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Содержимое текстового файла из репозитория GitLab-проекта. Без ref читается ветка по умолчанию. Для больших файлов используйте start_line и end_line (нумерация с 1, обе границы включительно) — так можно читать файл по частям. Без диапазона выводится не больше 15000 символов; при обрезке в конце указано, какие строки показаны и как запросить остальное. Заголовок содержит путь, ref, диапазон строк, общее число строк и размер. Бинарные файлы (изображения, архивы и т. п.) не выводятся: вместо содержимого возвращается пометка с размером и blob_id.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "end_line": {
          "description": "last line to return, inclusive",
          "type": "integer"
        },
        "path": {
          "description": "file path inside the repository, for example src/main.go",
          "type": "string"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "ref": {
          "description": "branch, tag or commit SHA; default the project's default branch",
          "type": "string"
        },
        "start_line": {
          "description": "first line to return, 1-based; use for big files",
          "type": "integer"
        }
      },
      "required": [
        "project",
        "path"
      ],
      "type": "object"
    },
    "name": "get_file_contents"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Один Merge Request GitLab-проекта по iid (номер после ! в GitLab): заголовок, state, draft, автор, ветки, описание (до 1500 символов), признак конфликтов, статус pipeline, число изменённых файлов и ссылка. Строка detailed_merge_status показывает, можно ли вливать MR, и что делать, если нельзя. Проверка слияния в GitLab асинхронная: сразу после создания MR статус checking или unchecked означает «проверка идёт», повторите вызов через несколько секунд. Вызывайте перед merge_merge_request. Diff показывает get_merge_request_diffs.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "iid": {
          "description": "merge request IID, the number after ! in GitLab",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        }
      },
      "required": [
        "project",
        "iid"
      ],
      "type": "object"
    },
    "name": "get_merge_request"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Diff Merge Request по файлам: iid — номер после ! в GitLab. Patch каждого файла ограничен 2000 символами, весь вывод — 15000. Если patch у файла не показан, рядом указана причина (too_large, collapsed, только переименование, только смена режима, пустой или бинарный файл) и как прочитать содержимое через get_file_contents. Если в MR больше 1000 файлов, GitLab отдаёт не все, и вывод говорит «часть файлов не вернулась». page и per_page листают файлы; подсказка внизу говорит, есть ли следующая страница.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "iid": {
          "description": "merge request IID, the number after ! in GitLab",
          "type": "integer"
        },
        "page": {
          "description": "page of files, default 1",
          "type": "integer"
        },
        "per_page": {
          "description": "files per page, default 20, max 100",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        }
      },
      "required": [
        "project",
        "iid"
      ],
      "type": "object"
    },
    "name": "get_merge_request_diffs"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Ключевые поля одного проекта GitLab: id, путь, ветка по умолчанию, видимость, архивный ли, дата последней активности, ссылка и описание. Проект задаётся числовым ID или путём group/subgroup/project.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "get_project"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Ветки GitLab-проекта: одна строка на ветку в виде «имя короткий-SHA [маркеры] заголовок последнего коммита». Маркеры: [default] — ветка по умолчанию; [protected] — ветка может быть защищена, писать в неё напрямую нельзя, коммитьте в свою ветку, созданную create_branch; [merged] — ветка уже влита. С search показываются только ветки, в имени которых есть эта подстрока. Для следующей страницы вызовите снова с page из подсказки внизу.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "page": {
          "description": "page number, default 1",
          "type": "integer"
        },
        "per_page": {
          "description": "items per page, default 20, max 100",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "search": {
          "description": "substring of the branch name to filter by",
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "list_branches"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "История коммитов GitLab-проекта: одна строка на коммит «короткий SHA, дата, автор, заголовок». По умолчанию берётся ветка по умолчанию проекта; ref задаёт другую ветку, тег или коммит. Фильтры: path — только коммиты, затрагивающие файл или каталог; since и until — границы дат в ISO 8601, например 2026-09-01 или 2026-09-01T12:00:00Z; author — часть имени или email автора. Полный SHA и diff коммита показывает get_commit. Для следующей страницы вызовите снова с page из подсказки внизу.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "author": {
          "description": "author name or email substring",
          "type": "string"
        },
        "page": {
          "description": "page number, default 1",
          "type": "integer"
        },
        "path": {
          "description": "only commits touching this file or directory",
          "type": "string"
        },
        "per_page": {
          "description": "items per page, default 20, max 100",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "ref": {
          "description": "branch, tag or commit SHA; default the project's default branch",
          "type": "string"
        },
        "since": {
          "description": "ISO 8601 date or date-time, for example 2026-09-01 or 2026-09-01T12:00:00Z",
          "type": "string"
        },
        "until": {
          "description": "ISO 8601 date or date-time, for example 2026-09-01 or 2026-09-01T12:00:00Z",
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "list_commits"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Заметки (обсуждение) Merge Request: iid — номер после ! в GitLab. Все заметки, новые первыми. Системные события GitLab (коммит добавлен, ветка обновлена, статус изменён) помечены [system] и занимают одну короткую строку. Заметка человека выводится как «#id @автор дата» и текст, текст длиннее 1000 символов обрезается. Для длинного обсуждения уменьшите per_page. Для следующей страницы вызовите снова с page из подсказки внизу.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "iid": {
          "description": "merge request IID, the number after ! in GitLab",
          "type": "integer"
        },
        "page": {
          "description": "page number, default 1",
          "type": "integer"
        },
        "per_page": {
          "description": "notes per page, default 20, max 100",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        }
      },
      "required": [
        "project",
        "iid"
      ],
      "type": "object"
    },
    "name": "list_merge_request_notes"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Merge Request'ы GitLab-проекта: одна строка на MR «!iid, state, [draft], заголовок, ветка-источник→целевая ветка, @автор». Без фильтров показываются только открытые MR (state=opened). state: opened, closed, merged или all. Фильтры: source_branch и target_branch — точные имена веток; author_username — логин автора; search — текст в заголовке и описании. Детали одного MR, включая возможность слияния, показывает get_merge_request. Для следующей страницы вызовите снова с page из подсказки внизу.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "author_username": {
          "description": "GitLab username of the MR author",
          "type": "string"
        },
        "page": {
          "description": "page number, default 1",
          "type": "integer"
        },
        "per_page": {
          "description": "items per page, default 20, max 100",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "search": {
          "description": "text to search in title and description",
          "type": "string"
        },
        "source_branch": {
          "description": "only MRs from this source branch",
          "type": "string"
        },
        "state": {
          "description": "opened (default), closed, merged or all",
          "type": "string"
        },
        "target_branch": {
          "description": "only MRs into this target branch",
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "list_merge_requests"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Список проектов GitLab, доступных по токену (по умолчанию только те, где вы участник). Одна строка на проект: id, путь, ветка по умолчанию, описание. Для следующей страницы вызовите снова с page из подсказки внизу.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "include_public": {
          "description": "also include public projects you are not a member of; default false = only your projects",
          "type": "boolean"
        },
        "page": {
          "description": "page number, default 1",
          "type": "integer"
        },
        "per_page": {
          "description": "items per page, default 20, max 100",
          "type": "integer"
        },
        "search": {
          "description": "filter by name or path substring",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_projects"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Дерево файлов репозитория GitLab-проекта: одна строка на элемент. Каталоги помечены «dir», файлы «file», подмодули «sub». По умолчанию показывается корень репозитория на ветке по умолчанию; чтобы заглянуть глубже, укажите path (каталог внутри репозитория), а чтобы выбрать другую ветку, тег или коммит — ref. С recursive=true выводятся все вложенные элементы с полными путями. Вывод ограничен 15000 символами. Для следующей страницы вызовите снова с page из подсказки внизу.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "page": {
          "description": "page number, default 1",
          "type": "integer"
        },
        "path": {
          "description": "directory inside the repository, default repository root",
          "type": "string"
        },
        "per_page": {
          "description": "items per page, default 20, max 100",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "recursive": {
          "description": "list all nested entries; output is capped at 15000 characters",
          "type": "boolean"
        },
        "ref": {
          "description": "branch, tag or commit SHA; default the project's default branch",
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "list_repository_tree"
  },
  {
    "annotations": {
      "destructiveHint": true,
      "idempotentHint": false,
      "readOnlyHint": false
    },
    "description": "Изменяет Merge Request: title, description, target_branch, state_event, draft. Меняются только непустые поля; пустое значение означает «не менять», поэтому описание нельзя очистить, а флаги нельзя сбросить пустым значением. state_event=close закрывает MR, reopen открывает закрытый заново. draft=true добавляет к заголовку префикс «Draft:»; чтобы снять Draft, передайте title без этого префикса (draft=false означает «не передано»). Запрос никогда не повторяется автоматически. Запись требует токен со scope api и роль Developer или выше.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "description": {
          "description": "new description in Markdown; empty means unchanged",
          "type": "string"
        },
        "draft": {
          "description": "true marks the MR as Draft; to remove Draft send title without the Draft: prefix",
          "type": "boolean"
        },
        "iid": {
          "description": "merge request IID, the number after ! in GitLab",
          "type": "integer"
        },
        "project": {
          "description": "numeric project ID as a string (\"12345\") or full path group/subgroup/project",
          "type": "string"
        },
        "state_event": {
          "description": "close or reopen",
          "type": "string"
        },
        "target_branch": {
          "description": "new target branch; empty means unchanged",
          "type": "string"
        },
        "title": {
          "description": "new title; empty means unchanged",
          "type": "string"
        }
      },
      "required": [
        "project",
        "iid"
      ],
      "type": "object"
    },
    "name": "update_merge_request"
  },
  {
    "annotations": {
      "idempotentHint": false,
      "readOnlyHint": true
    },
    "description": "Показывает текущего пользователя GitLab по токену и scopes токена. Используйте для быстрой проверки токена и прав.",
    "inputSchema": {
      "additionalProperties": false,
      "type": "object"
    },
    "name": "whoami"
  }
]
